# Active Task List

Last updated: 2026-09-26 · Branch: `202609`

Legend: `[ ]` todo · `[~]` in progress · `[x]` done (awaiting archive) ·
`[-]` won't do

**Rules**

1. Only open work lives here. When a task is done and verified, move it
   to [`TASK_ARCHIVE.md`](TASK_ARCHIVE.md) in the same commit, with its
   completion date, commit hash and evidence.
2. Every task names its **exit gate**: the check that proves it is done.
3. The v1.0.0 programme implements the approved design in
   [`UNIFIED_TOOLS_CONCEPT.md`](UNIFIED_TOOLS_CONCEPT.md) exactly as
   specified in [`docs/unified-tools/`](docs/unified-tools/README.md). The
   spec is the source of truth: any deviation is a spec change first (edit
   `scripts/build_unified_tool_specs.py`, regenerate, re-test), never an
   implementation shortcut.
4. Standing quality gates for every code task:
   - `uv run pytest tests/ -q --ignore=tests/test_integration.py` green;
   - `uv run pyright` shows no new errors;
   - `uvx ruff check` shows no new findings;
   - `uv run python scripts/build_unified_tool_specs.py --check` passes;
   - TDD: failing test first (steering `python.md`).

Completed history: see `TASK_ARCHIVE.md` (reliability/auth review T1–T11,
N1–N3; documentation DOC1–DOC2; design and specification D1–D5).

---

## A. v1.0.0 unified tools programme

Design: `UNIFIED_TOOLS_CONCEPT.md` §14 (phases 0–5). Target: 29 tools
(core 16, extended 7, admin 6), hard cut of all 85 legacy names, personal
accounts only.

### Phase 0 — Baseline

- [~] **U0.2** Build the golden-prompt evaluation harness (`evals/`):
  - 80–120 prompts: direct, indirect, ambiguous, competing-tool (list vs
    search, email_folder vs drive folder, update vs move) and no-tool;
  - a 25% held-out split;
  - a mocked Graph layer, so runs are deterministic and side-effect free;
  - a runner that presents a given tool surface to a Claude model (current
    default: `claude-sonnet-5`) and records the §16.2 metrics:
    - correct first tool;
    - first-try schema-valid arguments;
    - task success;
    - calls per task;
    - result tokens;
    - unconfirmed side-effect calls.

  *Gate:* harness runs end-to-end on a 10-prompt smoke set.

  Status: harness built in `evals/` (115 cases, 28 held out, fake Graph,
  runner, offline tests in `tests/test_evals_harness.py`). The live smoke
  run is waiting on Anthropic API credentials on this machine.
- [ ] **U0.3** Record the baseline on the 85-tool surface into
  `evals/results/baseline-v0.md`. *Gate:* metrics table committed.

### Phase 1 — Services layer (no behaviour change)

Move Graph logic out of `src/m365_mcp/tools/*.py` into
`src/m365_mcp/services/`. Legacy tools become thin callers. The tool
surface must be byte-identical, verified by `MCP_SERVER_TOOLS.md --check`.

All Phase 1 tasks are done (see `TASK_ARCHIVE.md`).

### Phase 2 — Foundations

All Phase 2 tasks are done (see `TASK_ARCHIVE.md`).

### Phase 3 — Implement the 29 tools

For each tool:
- implement the handler on the services;
- enforce every `validation_rules` entry with its exact error text;
- apply the confirm gate exactly as in `meta.confirm` / `confirm_rule`;
- return a projection that validates against `outputSchema`;
- make the spec examples pass against the mocked Graph;
- assert the Graph calls listed in `graph_calls`.

*Gate per tool:* its test module passes and its conformance row is green.

Core:
- [ ] **U3.1** `m365_list`: 8 resources; email filters build `$filter` with
  `$orderby` compatibility; event window via `calendarView`; folder and
  drive trees to `max_depth`; cursors.
- [ ] **U3.2** `m365_get`: 9 resources including `operation`; body
  truncation; attachments metadata; drive path lookup.
- [ ] **U3.3** `m365_search`:
  - email via server-side `$search` (fixes the recent-50-only bug);
  - events via `calendarView` window matching;
  - contacts via `startswith` plus email;
  - drive search;
  - multi-resource merge by recency with sub-cursors;
  - query escaping.
- [ ] **U3.4** `m365_get_content`: download (drive and attachment) to
  allowed paths; `download_url`; vCard 3.0 export.
- [ ] **U3.5** `m365_create`: email_folder, calendar, contact (with folder),
  contact_folder, drive folder (`conflictBehavior`).
- [ ] **U3.6** `m365_update`: typed email changes (categories
  add/remove/set, flag, importance, inference classification), folder
  rename, contact changes, drive rename; protected items refused.
- [ ] **U3.7** `m365_move`:
  - email and email folder `/move` (return the new ID);
  - contact move as create-then-delete, with a `copied_not_removed` path;
  - drive `parentReference` patch;
  - no retry on ambiguous failure.
- [ ] **U3.8** `m365_delete`: 8 resources; the organiser cancels meetings
  with attendees via `/cancel` and a comment; protected items refused;
  `recoverable` flag.
- [ ] **U3.9** `email_create_draft`: bcc, `body_format`, importance;
  attachment upload sessions for 3 MB or more.
- [ ] **U3.10** `email_send`: `mode=new` (`sendMail`, or draft-then-send
  for large attachments) and `mode=draft`; "outcome unknown" handling.
- [ ] **U3.11** `email_reply`: `mode=sender|all`; `createReply(All)` path
  for cc and attachments.
- [ ] **U3.12** `email_forward`: `/forward`, or the `createForward` path for
  cc, bcc and attachments.
- [ ] **U3.13** `calendar_create_event`: confirm iff attendees;
  `calendar_id`, all-day, reminder, `show_as`; `invitations_sent`.
- [ ] **U3.14** `calendar_update_event`: fetch first to detect attendees;
  attendee set/add/remove; organiser-only checks.
- [ ] **U3.15** `calendar_respond`: accept, tentative or decline; proposed
  time only with tentative/decline; confirm iff `send_response`.
- [ ] **U3.16** `calendar_find_availability`: `calendarView` busy blocks,
  `mailboxSettings.workingHours`, free-slot algorithm (gap and working
  hours, across days and DST).

Extended:
- [ ] **U3.17** `drive_upload`: simple PUT up to 4 MB, upload session above;
  `if_exists` behaviours; replace by `item_id`.
- [ ] **U3.18** `drive_copy`: 202 plus the monitor URL into the operation
  store.
- [ ] **U3.19** `drive_share`:
  - link mode: view, edit or embed (files only); password; expiry;
    `created` flag;
  - invite mode: 207 partial success per recipient;
  - root refused.
- [ ] **U3.20** `email_folder_mark_all_read`: batched PATCH, bounded,
  `remaining_unread`.
- [ ] **U3.21** `email_folder_empty`: batched DELETE, bounded, partial
  failures reported.
- [ ] **U3.22** `email_rule_manage`: create, update, set_enabled; reorder
  top/bottom/up/down/before/after; confirm when the rule forwards,
  redirects or deletes.
- [ ] **U3.23** `calendar_forward`.

Admin:
- [ ] **U3.24** `account_list`.
- [ ] **U3.25** `account_auth_begin` (session store).
- [ ] **U3.26** `account_auth_complete` (single poll, personal only).
- [ ] **U3.27** `admin_cache_get` (4 views).
- [ ] **U3.28** `admin_cache_invalidate` (typed scope).
- [ ] **U3.29** `admin_server_info`.

Parity and conformance:
- [ ] **U3.30** 85 parity tests, one per `legacy_mapping.json` row. Each
  proves the replacement completes the same practical task; rows with a
  `behaviour_change` assert the new behaviour. *Gate:* 85/85 green.
- [~] **U3.31** Extend the live read-only integration test to cover
  list/get/search for every resource on the personal account. *Gate:*
  passes against the real account.

  Status: `tests/test_integration_unified.py` written (list then get for
  all 8 resources, cursor paging, search per resource, drive root by path,
  availability; reads only). Run it yourself with
  `M365_MCP_LIVE_TESTS=1 uv run pytest tests/test_integration_unified.py -v`.
- [ ] **U3.32** Live `tools/list` conformance: exact spec equality per
  toolset combination; determinism across processes; admin hidden by
  default. *Gate:* green in CI.

### Phase 4 — Evaluation

- [ ] **U4.1** Run the golden prompts (U0.2) on the new surface. Targets
  (concept §16.2):
  - correct first tool ≥ baseline + 10 points;
  - schema-valid arguments ≥ 95%;
  - task success ≥ baseline;
  - list/search result median ≤ 2k tokens;
  - 0 unconfirmed side effects.

  Tune descriptions only through the spec builder.
- [ ] **U4.2** Record the results in `docs/unified-tools/EVALUATION.md`
  (baseline vs new, held-out set reported separately). *Gate:* all targets
  met, or a documented decision.

### Phase 5 — Cut-over (hard cut, 1.0.0)

- [ ] **U5.1 Remove legacy code:**
  - `src/m365_mcp/tools/{account,cache_tools,calendar,contact,email,email_folders,email_rules,file,folder,search,server}.py`;
  - `search_router.py`;
  - `account_type.py`.
- [ ] **U5.2 Remove or replace legacy tests:** `test_calendar_*`,
  `test_contact_*`, `test_email_*`, `test_file_*`, `test_folder_validation`,
  `test_search_validation`, `test_tool_surface`,
  `test_tool_signature_policy`, `test_tool_confirmation`,
  `test_mutation_cache_invalidation`, `test_tool_caching`,
  `test_account_type`, and the legacy parts of `test_account_validation`.
  Keep the auth and graph-client tests. *Gate:* no test imports a removed
  module; the suite is green.
- [ ] **U5.3 Steering docs** (concept §17):
  - `mcp-server.md`: breaking-change policy; typed `outputSchema`; no
    internal fields; confirm rules.
  - `tool-names.md`: categories `m365_`, `email_`, `calendar_`, `drive_`,
    `account_`, `admin_`; drop the emoji rule; use-when template.
  - `structure.md`: `services/`, `tool_specs/`, `projections.py`,
    `cursors.py`, `errors.py`.
  - `tech.md`: personal only, `M365_MCP_TOOLSETS`, `consumers`.
  - `python.md`: done in H4 (ruff, pyright, `>=` ranges with `uv.lock`).
- [ ] **U5.4 Project docs:**
  - `CLAUDE.md`: architecture, cache tools list, patterns;
  - `README.md`, `QUICKSTART.md`, `SECURITY.md`, `FILETREE.md`;
  - `.env.example`: `M365_MCP_TOOLSETS`, tenant default `consumers`;
  - `docs/cache_*.md`: tool names;
  - `CHANGELOG.md` 1.0.0 entry listing every breaking change from concept
    §15.
- [ ] **U5.5 Tool reference:** update the `generate_tools_doc.py`
  `CATEGORIES`, regenerate `MCP_SERVER_TOOLS.md`, and remove the obsolete
  `ARG_OVERRIDES` / `IMPLEMENTATION_NOTES`. *Gate:* `--check` passes.
- [ ] **U5.6 Version:** 1.0.0 in `pyproject.toml`, `admin_server_info`,
  and `uv.lock`.
- [ ] **U5.7 Release gates:**
  - full suite, pyright and ruff (no findings in new code);
  - both `--check` scripts;
  - U4 targets met;
  - a live smoke test of every tool on the personal account (write tests
    against a disposable folder, draft, event and contact);
  - then PR to `master` and tag `v1.0.0`.

---

## B. Independent tracks (not blocked by the programme)


- [ ] **H2** MCP 2026-07-28 support: track FastMCP/`mcp` releases (current
  maximum 2025-06-18); upgrade when available; add a protocol-version
  interop test matrix. *Gate:* negotiation test passes for each supported
  version.

  Status (2026-09-26): `mcp` 2.2.0 and FastMCP 4.0.10 support 2026-07-28,
  but FastMCP 4 is a major API rewrite. Decision: upgrade right after U5.1
  (only the unified registry left to port), before the U5.7 release gates.
- [ ] **H3** Client configuration guide: per-client setup for Claude
  Desktop/Code, OpenAI (`allowed_tools`, `defer_loading`) and Gemini
  (`allowed_tools`, Streamable HTTP); mapping `M365_MCP_TOOLSETS` to
  eager/deferred sets. *Gate:* documented in the README.

- [ ] **H5** Existing lint debt in legacy files (TRY002, BLE001, I001,
  UP035). Resolved by deletion in U5.1; any survivors fixed afterwards.
  *Gate:* `uvx ruff check .` clean.
- [ ] **R1** Keep the personal refresh token alive: schedule
  `uv run authenticate.py --re-auth <email>` weekly (Windows Task
  Scheduler). Exit code 1 signals that sign-in is required. *Gate:*
  scheduled task created and a run observed.

---

## C. Audit and best-practice traceability

Where each open finding from `202609_MCP_BEST_PRACTICES_REPORT.md`, and each
checklist item in `MCP_BEST_PRACTICES.md`, is resolved. Audit items are
**superseded** where the v1.0.0 design replaces the legacy surface, rather
than being fixed in the legacy tools.

| Audit item | Resolution |
|---|---|
| BP1 share and forwarding-rule guards | Spec: `drive_share` confirm always, no default scope; `email_rule_manage` conditional confirm → U3.19, U3.22 |
| BP2 actionable Graph errors | U2.7 |
| BP3 compact list results | Spec projections → U2.5, U3.1 |
| BP4 admin tools hidden, core set | Spec tiers → U2.2 |
| BP5 evaluation harness | U0.2, U0.3, U4 |
| BP6 HTTP hardening | H1 |
| BP7 parameter descriptions, enums, bounds | Done in spec (D3; tested) → enforced at runtime by U2.2/U2.3 |
| BP8 use-when descriptions, steering | Done in spec (D3) → U5.3 |
| BP9 typed outputs, cursors, summary | Spec output schemas → U2.4–U2.6 |
| BP10 merge overlapping tools | Spec 29-tool catalog → Phase 3 |
| BP11 calendar confirm, deny-list, opaque auth handle | U3.13–U3.15, U2.13, U2.11 |
| BP12 audit log, instructions, deadlines | U2.15, U2.9 |
| BP13 untrusted content | U2.17 |
| BP14 rate limiting | U2.14 |
| BP15 async operation handles | U2.16, U3.18 |
| BP16 MCP 2026-07-28 | H2 |
| BP17 namespace cleanup | Spec names (`email_folder_*`, `drive_*`, resource `drive_item`) → Phase 3 |
| BP18 legacy delete descriptions | Superseded: new descriptions state recycle-bin and cancellation behaviour; legacy removed in U5.1 |
| BP19 `file_delete` undocumented parameters | Superseded: `m365_delete` is fully documented; legacy removed in U5.1 |

| `MCP_BEST_PRACTICES.md` checklist item | Where satisfied |
|---|---|
| P0 evaluation suite before refactoring | U0.2–U0.3 |
| P0 distinct intents, no overlapping tools | Spec catalog (D2/D3) → Phase 3 |
| P0 stable snake_case names | Spec names (tested pattern) |
| P0 use / do-not-use descriptions | Spec descriptions (D3) → tuned in U4.1 |
| P0 tight input schemas | Spec (tested: descriptions, bounds, enums, closed objects) → U2.3 |
| P0 authorisation and business validation every call | U2.3 validation rules, U2.10 account resolution, Graph delegated scopes |
| P0 accurate read/write/destructive classification; approval-friendly | Spec annotations and meta (tested) → confirm gates in Phase 3 |
| P0 Streamable HTTP | Already implemented; hardened by H1 |
| P0 stateless requests | Already stateless; explicit handles: cursors U2.6, auth sessions U2.11, operations U2.16 |
| P1 outputSchema + structuredContent + short text | Spec (D3) → U2.4 |
| P1 pagination and filtering on large results | Spec → U2.6, U3.1, U3.3 |
| P1 semantic labels plus needed IDs | Projections U2.5 |
| P1 deterministic, cacheable tools/list | U2.2, U3.32 |
| P1 reduced eager inventory | Tiers and `M365_MCP_TOOLSETS` U2.2; client guide H3 |
| P1 definitions out of hot paths | Static spec JSON loaded once (U2.1–U2.2) |
| P1 actionable isError results | U2.3, U2.7 |
| P1 deadlines and retry policy | Retry policy done (archive T6–T9a); deadlines U2.9 |
| P1 long operations via handles | U2.16, U3.18; bounded bulk ops U3.20–U3.21 |
| P1 sanitise untrusted content | U2.17 |
| MUST rate-limit invocations (spec) | U2.14 |
| P2 provider-specific list reuse / deferred loading | H3 |
| P2 protocol-generation interop testing | H2 |
