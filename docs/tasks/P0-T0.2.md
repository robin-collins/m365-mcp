# Phase 0 - T0.2 Completion Report

Task: Add regression-test scaffolding for Windows-sensitive validation and cache
file teardown.

Source: `docs/audit_remediation_task_list.md`, T0.2.

## Summary

Added focused scaffolding tests for the Windows path validation and cache
teardown risks identified by the audit. These tests document the intended
behavior without forcing the implementation fixes into this setup task.

## Changes Made

- Added `tests/test_windows_path_scaffolding.py`.
  - Confirms ADS-style paths remain rejected.
  - Adds a strict `xfail` for normal Windows absolute paths under an allowed
    root, matching audit Finding 2.
- Added `tests/test_cache_teardown_scaffolding.py`.
  - Adds a strict `xfail` requiring `CacheManager.close()`.
  - Adds a skipped deletion test that will become active once explicit cache
    teardown exists, covering Findings 8 and 12.

## Commands Run

```powershell
uv run pytest tests/test_windows_path_scaffolding.py -q
uv run pytest tests/test_cache_teardown_scaffolding.py -q
```

## Results

- `tests/test_windows_path_scaffolding.py`: passed with 1 pass and 1 expected
  failure.
- `tests/test_cache_teardown_scaffolding.py`: passed with 1 skip and 1 expected
  failure.

## Verification Status

T0.2 is complete. Later implementation tasks must convert the staged `xfail` and
skip coverage into passing regression tests when the corresponding fixes land.
