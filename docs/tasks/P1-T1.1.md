# Phase 1 - T1.1 Completion Report

Task: Fix account device-flow startup.

Source: `docs/audit_remediation_task_list.md`, T1.1.

## Summary

Fixed `account_authenticate` so it respects the real `auth.get_app()` contract.
The tool now unpacks `(app, tenant_id)` and starts device flow through
`auth._initiate_device_flow`, preserving the retry behavior used by the
interactive authentication path.

## Changes Made

- Updated `src/m365_mcp/tools/account.py` to unpack `auth.get_app()`.
- Routed startup through `auth._initiate_device_flow(app, tenant_id)`.
- Updated account authentication tests so `auth.get_app` is mocked with the real
  tuple shape.
- Added an assertion that the device flow uses `auth.DEVICE_FLOW_SCOPES`.

## Commands Run

```powershell
uv run pytest tests/test_account_validation.py -q
```

## Results

- `tests/test_account_validation.py`: passed, 6 tests.

## Verification Status

T1.1 is complete. Remaining account completion tuple handling is covered by
T1.2.
