# Phase 1 - T1.2 Completion Report

Task: Fix account device-flow completion.

Source: `docs/audit_remediation_task_list.md`, T1.2.

## Summary

Fixed `account_complete_auth` so it also respects the tuple returned by
`auth.get_app()`. Completion now polls the device flow on the unpacked MSAL app
instead of on the `(app, tenant_id)` tuple.

## Changes Made

- Updated `src/m365_mcp/tools/account.py` to unpack `auth.get_app()` in
  `account_complete_auth`.
- Updated completion tests so successful and pending completion paths mock
  `auth.get_app()` with the real tuple shape.

## Commands Run

```powershell
uv run pytest tests/test_account_validation.py -q
```

## Results

- `tests/test_account_validation.py`: passed, 6 tests.

## Verification Status

T1.2 is complete. Phase 1 gate remains to be run in T1.3.
