# P5-T5.5 - Add explicit cache teardown

## Audit references

- Finding 8: cache database lifecycle needed explicit teardown to avoid locked temporary files on Windows.
- Finding 12: tests deleting temporary database files needed to close pooled connections first.

## Changes completed

- Added `CacheManager.close()` to close all pooled SQLite connections under the existing pool lock.
- Registered the singleton cache manager for `atexit` teardown in `cache_tools`.
- Updated cache-related fixtures and direct manager constructions to call `close()` before temporary database cleanup.
- Fixed the error-discard path so closing a failed connection no longer suppresses the original exception.

## Verification

- `uv run pytest tests/test_cache_teardown_scaffolding.py tests/test_cache.py tests/test_cache_tools.py tests/test_tool_caching.py tests/test_mutation_cache_invalidation.py -q`
  - Result: 58 passed.
- `uvx ruff check src/m365_mcp/cache.py src/m365_mcp/tools/cache_tools.py tests/test_cache.py tests/test_cache_tools.py tests/test_tool_caching.py tests/test_mutation_cache_invalidation.py tests/test_background_worker.py tests/test_cache_warming.py`
  - Result: passed.

## Notes

- This completes the T5.5 dependency for cache corruption recovery and later background-worker lifecycle wiring.
