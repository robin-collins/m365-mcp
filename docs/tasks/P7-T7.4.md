# P7-T7.4 Refresh Public Documentation After Behavior Changes

## Status

Completed.

## Audit References

- N2: cache warming lifecycle and stale-refresh behavior.
- N14: public tool surface and modular tool layout.
- N15: signature policy drift.
- Finding 11: stale tool layout documentation.

## Changes

- Added root `QUICKSTART.md` and `SECURITY.md`, matching existing README links.
- Updated README tool names to the current 85-tool public surface and replaced old unprefixed examples.
- Documented the selected compatibility policy: account-scoped tools require `account_id`, but existing public parameter order is preserved.
- Updated cache warming docs to describe the actual `M365_MCP_CACHE_WARMING=true` opt-in lifecycle, including inactive default status and enabled stale-refresh queue behavior.
- Updated cache security docs for SQLCipher fail-fast behavior, explicit plaintext mode, keyring/env/generated key order, and ephemeral-key warnings.
- Removed public references to deleted cache migration utilities and old cache task tool names.
- Refreshed steering guidance and `.env.example` for interactive auth, cache warming, encryption keys, and modular tool naming.

## Verification

- Searched README, root setup/security docs, steering docs, FILETREE, and cache docs for stale cache migration, cache task, cache warming, old tool-name, and old signature-policy language; no stale matches remained.
- Ran `git diff --check`; passed with no whitespace errors.

## Notes

- This task is documentation-only. No runtime tests were required beyond the documentation consistency checks above.
