# P8-T8.1 Run The Full Automated Suite

## Status

Completed with expected local integration failures.

## Verification

- `uv run pytest tests/ -v`
  - Result: 387 passed, 41 failed.
  - Runtime: 173.01 seconds.

## Failure Summary

All failures were live MCP integration tests that start the server subprocess:

- `tests/test_email_folders_integration.py`: 7 failures.
- `tests/test_integration.py`: 34 failures.

Each failed subprocess exited during startup with:

```text
M365_MCP_CLIENT_ID environment variable is required
```

The client then reported `McpError('Connection closed')` while initializing the
stdio session.

## Notes

- This matches the known local integration limitation: the full live suite
  requires a usable `M365_MCP_CLIENT_ID` and authenticated account state.
- The non-live portion of the suite passed: 387 tests.
- The command generated runtime log files under `logs/`, which are ignored by
  git and were not committed.
