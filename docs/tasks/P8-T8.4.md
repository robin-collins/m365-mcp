# P8-T8.4 Verify Windows-Specific Regressions

## Status

Completed.

## Audit References

- Finding 2: Windows alternate-data-stream path validation.
- Finding 3: timezone validation on Windows.
- Finding 8: explicit cache teardown.
- Finding 12: Windows test teardown `PermissionError` risk.

## Verification

Ran:

```text
uv run pytest tests/test_windows_path_scaffolding.py tests/test_validators.py tests/test_calendar_validation.py tests/test_cache_teardown_scaffolding.py -q
```

Result:

```text
29 passed in 3.82s
```

## Coverage

- Normal Windows absolute paths under an allowed root are accepted.
- Windows alternate data stream path segments are rejected.
- `UTC` timezone validation succeeds through the validator and calendar paths.
- Cache manager `close()` releases a temporary cache DB so it can be deleted.

## Notes

- No code changes were needed for this gate.
