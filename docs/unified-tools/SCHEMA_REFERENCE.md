# Unified Tools — Schema Reference

Specification version **1.0.0**. Generated by `scripts/build_unified_tool_specs.py` from the same source as `docs/unified-tools/tools/*.json`. **Do not edit by hand.**

The JSON files are the implementation source of truth: the server's `tools/list` must reproduce each file's `name`, `title`, `description`, `annotations`, `inputSchema` and `outputSchema` exactly (see docs/unified-tools/README.md).

## Tools

| # | Tool | Tier | Safety | Confirm | Replaces |
|---|---|---|---|---|---|
| 1 | [`m365_list`](#m365_list) | core | safe | never | `email_list`, `emailfolders_list`, `emailfolders_get_tree`, `emailrules_list`, `calendar_list_events`, `calendar_list_calendars`, `contact_list`, `file_list`, `folder_list`, `folder_get_tree` |
| 2 | [`m365_get`](#m365_get) | core | safe | never | `email_get`, `emailfolders_get`, `emailrules_get`, `calendar_get_event`, `contact_get`, `folder_get`, `file_get (details)` |
| 3 | [`m365_search`](#m365_search) | core | safe | never | `search_emails`, `search_events`, `search_contacts`, `search_files`, `search_unified` |
| 4 | [`m365_get_content`](#m365_get_content) | core | moderate | never | `file_get (download)`, `file_download_url`, `email_get_attachment`, `contact_export` |
| 5 | [`m365_create`](#m365_create) | core | moderate | never | `emailfolders_create`, `calendar_create_calendar`, `contact_create`, `contact_create_list`, `folder_create` |
| 6 | [`m365_update`](#m365_update) | core | moderate | never | `email_update`, `email_mark_read`, `email_flag`, `email_add_category`, `emailfolders_rename`, `contact_update`, `file_rename`, `folder_rename` |
| 7 | [`m365_move`](#m365_move) | core | moderate | never | `email_move`, `email_archive`, `emailfolders_move`, `contact_add_to_list`, `file_move`, `folder_move` |
| 8 | [`m365_delete`](#m365_delete) | core | critical | always | `email_delete`, `emailfolders_delete`, `emailrules_delete`, `calendar_delete_event`, `calendar_delete_calendar`, `contact_delete`, `file_delete`, `folder_delete` |
| 9 | [`email_create_draft`](#email_create_draft) | core | moderate | never | `email_create_draft` |
| 10 | [`email_send`](#email_send) | core | dangerous | always | `email_send` |
| 11 | [`email_reply`](#email_reply) | core | dangerous | always | `email_reply`, `email_reply_all` |
| 12 | [`email_forward`](#email_forward) | core | dangerous | always | `email_forward` |
| 13 | [`calendar_create_event`](#calendar_create_event) | core | dangerous | conditional | `calendar_create_event` |
| 14 | [`calendar_update_event`](#calendar_update_event) | core | dangerous | conditional | `calendar_update_event` |
| 15 | [`calendar_respond`](#calendar_respond) | core | dangerous | conditional | `calendar_respond_event`, `calendar_propose_new_time` |
| 16 | [`calendar_find_availability`](#calendar_find_availability) | core | safe | never | `calendar_check_availability`, `calendar_get_free_busy` |
| 17 | [`drive_upload`](#drive_upload) | extended | moderate | never | `file_create`, `file_update` |
| 18 | [`drive_copy`](#drive_copy) | extended | moderate | never | `file_copy` |
| 19 | [`drive_share`](#drive_share) | extended | dangerous | always | `file_share` |
| 20 | [`email_folder_mark_all_read`](#email_folder_mark_all_read) | extended | moderate | never | `emailfolders_mark_all_as_read` |
| 21 | [`email_folder_empty`](#email_folder_empty) | extended | critical | always | `emailfolders_empty` |
| 22 | [`email_rule_manage`](#email_rule_manage) | extended | dangerous | conditional | `emailrules_create`, `emailrules_update`, `emailrules_move_top`, `emailrules_move_bottom`, `emailrules_move_up`, `emailrules_move_down` |
| 23 | [`calendar_forward`](#calendar_forward) | extended | dangerous | always | `calendar_forward_event` |
| 24 | [`account_list`](#account_list) | admin | safe | never | `account_list` |
| 25 | [`account_auth_begin`](#account_auth_begin) | admin | moderate | never | `account_authenticate` |
| 26 | [`account_auth_complete`](#account_auth_complete) | admin | moderate | never | `account_complete_auth` |
| 27 | [`admin_cache_get`](#admin_cache_get) | admin | safe | never | `cache_get_stats`, `cache_task_list`, `cache_task_get_status`, `cache_warming_status` |
| 28 | [`admin_cache_invalidate`](#admin_cache_invalidate) | admin | moderate | never | `cache_invalidate` |
| 29 | [`admin_server_info`](#admin_server_info) | admin | safe | never | `server_get_version` |

<a id="m365_list"></a>

## `m365_list` — List Items

Browse items of one type in a known place: emails in a mail folder, events in a time window, calendars, contacts, contact folders, mail folders, inbox rules, or files in a OneDrive folder (optionally as a tree). Use this when you know where to look. Use m365_search to find items by text, and m365_get when you already have an ID. Returns compact items plus next_cursor for more.

- **Tier:** core · **Category:** m365 · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` | yes | — | — | Type of item to list. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `container_id` | string | no | — | min length 1, max length 1024 | Where to list. email: mail folder ID or alias (default inbox). email_folder: parent folder (default: top level). event: calendar ID (default: your default calendar). contact: contact folder ID (default: all contacts). drive_item: parent folder ID (default: OneDrive root). Not used for calendar, contact_folder and email_rule. |
| `path` | string | no | — | min length 1, max length 2048 | drive_item only: OneDrive folder path instead of container_id, e.g. /Documents/Tax. |
| `email_filter` | object | no | — | min fields 1 | email only: filters applied by Microsoft 365 (all must match). |
| `email_filter.unread` | boolean | no | — | — | Only unread (true) or only read (false). |
| `email_filter.from` | string (email) | no | — | min length 3, max length 320 | Email address, e.g. jane@example.com. |
| `email_filter.received_after` | string (date-time) | no | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `email_filter.received_before` | string (date-time) | no | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `email_filter.has_attachments` | boolean | no | — | — | Only messages with attachments. |
| `email_filter.importance` | `low` \| `normal` \| `high` | no | — | — | Message importance. |
| `email_filter.flagged` | boolean | no | — | — | Only flagged messages. |
| `email_filter.category` | string | no | — | min length 1, max length 255 | Only messages with this category. |
| `start` | string (date-time) | no | — | — | event only: window start (default now). RFC 3339 with offset. |
| `end` | string (date-time) | no | — | — | event only: window end (default start + 7 days; at most 366 days after start). |
| `item_type` | `file` \| `folder` \| `all` | no | `"all"` | — | drive_item only: which kinds of item to return. |
| `recursive` | boolean | no | `false` | — | email_folder and drive_item only: return a nested tree of folders instead of one level. |
| `max_depth` | integer | no | `3` | min 1, max 10 | Tree depth when recursive=true. |
| `include_hidden` | boolean | no | `false` | — | email_folder only: include hidden folders. |
| `limit` | integer | no | `20` | min 1, max 50 | Maximum number of items to return. |
| `cursor` | string | no | — | min length 1, max length 8192 | next_cursor from the previous page; keep all other arguments the same. |
| `refresh` | boolean | no | `false` | — | Bypass the cache and fetch fresh data. |

### Output

Page of items of the requested resource type.

| Field | Type |
|---|---|
| `resource` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` |
| `items` | array of [email_summary](#type-email_summary) or [email_folder](#type-email_folder) or [email_rule](#type-email_rule) or [event_summary](#type-event_summary) or [calendar](#type-calendar) or [contact](#type-contact) or [contact_folder](#type-contact_folder) or [drive_item](#type-drive_item) |
| `next_cursor` | string or null |
| `has_more` | boolean |
| `summary` | string |

### Microsoft Graph calls

- email: GET /me/mailFolders/{id}/messages ($filter from email_filter, $orderby receivedDateTime desc, $select projection)
- email_folder: GET /me/mailFolders[/{id}/childFolders] (recursive: walk childFolders to max_depth)
- email_rule: GET /me/mailFolders/inbox/messageRules
- event: GET /me/calendars/{id}/calendarView?startDateTime&endDateTime
- calendar: GET /me/calendars
- contact: GET /me/contacts or /me/contactFolders/{id}/contacts
- contact_folder: GET /me/contactFolders
- drive_item: GET /me/drive/items/{id}/children or /me/drive/root:{path}:/children (recursive: walk folders)

### Server-side validation

| Rule | Error returned |
|---|---|
| Parameters that do not apply to the chosen resource are rejected. | Invalid email_filter: only valid with resource='email'. Expected: remove email_filter or use resource='email' |
| container_id and path are mutually exclusive. | Invalid path: cannot be combined with container_id. Expected: one of container_id or path |
| event window: end must be after start and at most 366 days later. | Invalid end: must be after start and within 366 days. Expected: a later RFC 3339 date-time |
| received_before must be after received_after. | Invalid received_before: must be after received_after |
| cursor must come from the same request (resource, account and filters) and be less than 24 hours old. | Invalid cursor: it does not match this request. Expected: repeat the call without cursor |
| Unknown mail folder aliases are rejected. | Invalid container_id 'Inbx': unknown folder alias. Expected: a folder ID or one of inbox, sent, drafts, deleted, junk, archive |

### Example — Unread emails from one sender this month

```json
{
  "arguments": {
    "resource": "email",
    "email_filter": {
      "unread": true,
      "from": "billing@example.com",
      "received_after": "2026-09-01T00:00:00+09:30"
    },
    "limit": 1
  },
  "structuredContent": {
    "resource": "email",
    "items": [
      {
        "id": "AQMkADAwATM3ZmYAZS1k",
        "conversation_id": "AQQkADAwATM3ZmYAZS1",
        "subject": "Your September invoice",
        "from": {
          "name": "Example Billing",
          "address": "billing@example.com"
        },
        "to": [
          {
            "name": "Robin",
            "address": "robin@example.com"
          }
        ],
        "cc": [],
        "received_at": "2026-09-20T08:15:00+09:30",
        "is_read": false,
        "has_attachments": true,
        "importance": "normal",
        "flag_status": "not_flagged",
        "categories": [],
        "preview": "Your invoice for September is attached.",
        "folder_id": "AQMkADAwATM3ZmYAZS1kAC4AAAM"
      }
    ],
    "next_cursor": "eyJ2IjoxLCJyIjoiZW1haWwifQ",
    "has_more": true,
    "summary": "Returned 1 email from inbox; more available (pass next_cursor)."
  }
}
```

<a id="m365_get"></a>

## `m365_get` — Get Item

Read one item whose ID you already have: an email with its body, an event with attendees, a contact, a mail or contact folder, an inbox rule, a calendar, a OneDrive item's details, or the status of a copy started with drive_copy (resource='operation'). Use m365_list or m365_search to find IDs. Use m365_get_content to download files or attachments. Returns the item.

- **Tier:** core · **Category:** m365 · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` \| `operation` | yes | — | — | Type of item. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `id` | string | no | — | min length 1, max length 1024 | Item ID from a previous result (for operation: the operation_id). Aliases are accepted for email_folder, calendar (default) and drive_item (root). |
| `path` | string | no | — | min length 1, max length 2048 | drive_item only: OneDrive path instead of id. |
| `include_body` | boolean | no | `true` | — | email and event: include the body text. |
| `body_max_chars` | integer | no | `20000` | min 500, max 100000 | email and event: truncate the body after this many characters. |
| `refresh` | boolean | no | `false` | — | Bypass the cache and fetch fresh data. |

### Output

The requested item.

| Field | Type |
|---|---|
| `resource` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` \| `operation` |
| `item` | [email_detail](#type-email_detail) or [email_folder](#type-email_folder) or [email_rule](#type-email_rule) or [event_detail](#type-event_detail) or [calendar](#type-calendar) or [contact](#type-contact) or [contact_folder](#type-contact_folder) or [drive_item](#type-drive_item) or [operation](#type-operation) |
| `summary` | string |

### Microsoft Graph calls

- email: GET /me/messages/{id}?$expand=attachments($select=id,name,size,contentType,isInline) with Prefer: outlook.body-content-type="text"
- email_folder: GET /me/mailFolders/{id}
- email_rule: GET /me/mailFolders/inbox/messageRules/{id}
- event: GET /me/events/{id}
- calendar: GET /me/calendars/{id} or /me/calendar
- contact: GET /me/contacts/{id}
- contact_folder: GET /me/contactFolders/{id}
- drive_item: GET /me/drive/items/{id} or /me/drive/root:{path}
- operation: GET {stored monitor URL} (no Authorization header)

### Server-side validation

| Rule | Error returned |
|---|---|
| Exactly one of id or path (path only for drive_item). | Invalid id: required unless resource='drive_item' with path |
| Unknown or expired operation IDs are rejected. | Invalid id: unknown operation. Expected: an operation_id returned by drive_copy in the last 24 hours |

### Example — Check a copy operation

```json
{
  "arguments": {
    "resource": "operation",
    "id": "op_7f3c2a"
  },
  "structuredContent": {
    "resource": "operation",
    "item": {
      "operation_id": "op_7f3c2a",
      "status": "completed",
      "percent_complete": 100,
      "resource_id": "01ABCDEF2345",
      "error": null
    },
    "summary": "Copy completed; new item id 01ABCDEF2345."
  }
}
```

<a id="m365_search"></a>

## `m365_search` — Search

Find emails, events, contacts or OneDrive files by free text when you do not know where they are. Search one type or several at once. Do not use it to browse a folder (m365_list) or open a known ID (m365_get). Returns matching items labelled by resource, newest first, plus next_cursor.

- **Tier:** core · **Category:** m365 · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `query` | string | yes | — | min length 1, max length 256 | Words to search for. Plain text; the server handles quoting. |
| `resources` | array of `email` \| `event` \| `contact` \| `drive_item` | no | — | min items 1, max items 4, unique | Which item types to search. Default: all four. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `email_folder_id` | string | no | — | min length 1, max length 1024 | Mail folder ID, or one of the aliases inbox, sent, drafts, deleted, junk, archive. |
| `event_start` | string (date-time) | no | — | — | Event search window start (default: 90 days ago). |
| `event_end` | string (date-time) | no | — | — | Event search window end (default: 365 days ahead). |
| `limit` | integer | no | `20` | min 1, max 50 | Maximum number of items to return. |
| `cursor` | string | no | — | min length 1, max length 8192 | next_cursor from the previous page; keep all other arguments the same. |

### Output

Matches across the requested resource types.

| Field | Type |
|---|---|
| `query` | string |
| `items` | array of object |
| `next_cursor` | string or null |
| `has_more` | boolean |
| `summary` | string |

### Microsoft Graph calls

- email: GET /me/messages?$search="{query}" (or /me/mailFolders/{id}/messages) — whole mailbox, paged
- event: GET /me/calendarView over the window, matched server-side on subject, location, preview and organiser
- contact: GET /me/contacts?$filter=startswith(displayName,'q') or startswith(givenName,'q') or startswith(surname,'q'), plus email match
- drive_item: GET /me/drive/root/search(q='{query}')

### Server-side validation

| Rule | Error returned |
|---|---|
| email_folder_id only when resources includes email; event_start / event_end only when resources includes event. | Invalid email_folder_id: only valid when resources includes 'email' |
| event_end after event_start, span at most 731 days. | Invalid event_end: must be after event_start and within 731 days |
| The query is escaped for OData string literals and $search quoting. | (no error; injection-safe by construction) |

### Example — Search mail and files together

```json
{
  "arguments": {
    "query": "tax return",
    "resources": [
      "email",
      "drive_item"
    ],
    "limit": 1
  },
  "structuredContent": {
    "query": "tax return",
    "items": [
      {
        "resource": "drive_item",
        "item": {
          "id": "01ABCDEF2345",
          "name": "Tax return 2026.pdf",
          "item_type": "file",
          "path": "/Documents/Tax",
          "parent_id": "01ABCDEF0001",
          "size": 482113,
          "modified_at": "2026-08-30T10:00:00+09:30",
          "created_at": "2026-08-30T10:00:00+09:30",
          "mime_type": "application/pdf",
          "child_count": null,
          "web_url": "https://onedrive.live.com/?id=01ABCDEF2345",
          "shared": false,
          "children": null
        }
      }
    ],
    "next_cursor": "eyJ2IjoxLCJyIjoic2VhcmNoIn0",
    "has_more": true,
    "summary": "Returned 1 match (drive_item: 1); more available."
  }
}
```

<a id="m365_get_content"></a>

## `m365_get_content` — Get Content

Get the content of an item rather than its details: save a OneDrive file or an email attachment to a local file (mode='download'), get a temporary download link for a OneDrive file (mode='download_url'), or export a contact as a vCard (mode='vcard'). Writes to the local disk only inside the allowed folders. Use m365_get for item details.

- **Tier:** core · **Category:** m365 · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `drive_item` \| `email` \| `contact` | yes | — | — | Type of item. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `id` | string | yes | — | min length 1, max length 1024 | OneDrive item ID, email ID or contact ID. |
| `mode` | `download` \| `download_url` \| `vcard` | yes | — | — | download: drive_item file or email attachment to save_path. download_url: drive_item file only. vcard: contact only. |
| `attachment_id` | string | no | — | min length 1, max length 1024 | email + download only: attachment ID from m365_get(resource='email'). |
| `save_path` | string | no | — | min length 1, max length 4096 | Local file path inside the server's allowed folders. Hidden and secret files are refused. |
| `overwrite` | boolean | no | `false` | — | Replace save_path if it already exists. |

### Output

Content result; fields that do not apply to the mode are null. download_url is a short-lived pre-authenticated link: treat it as a secret.

| Field | Type |
|---|---|
| `resource` | `drive_item` \| `email` \| `contact` |
| `id` | string |
| `mode` | `download` \| `download_url` \| `vcard` |
| `saved_path` | string or null |
| `size` | integer or null |
| `mime_type` | string or null |
| `download_url` | string or null |
| `vcard` | string or null |
| `summary` | string |

### Microsoft Graph calls

- drive_item download: GET /me/drive/items/{id} then stream @microsoft.graph.downloadUrl to save_path (≤ MCP_FILE_DOWNLOAD_MAX_MB)
- drive_item download_url: GET /me/drive/items/{id}?$select=id,name,size,@microsoft.graph.downloadUrl
- email download: GET /me/messages/{id}/attachments/{attachment_id} (fileAttachment, ≤ 25 MB)
- contact vcard: GET /me/contacts/{id}, rendered as vCard 3.0

### Server-side validation

| Rule | Error returned |
|---|---|
| mode must suit the resource (download: drive_item or email; download_url: drive_item; vcard: contact). | Invalid mode 'vcard': not valid for resource 'drive_item'. Expected: download or download_url |
| save_path is required for download and must be inside the allowed roots and not deny-listed. | Invalid save_path: outside allowed folders. Expected: a path under the working directory, temp directory or MCP_FILE_ALLOWED_ROOTS |
| attachment_id is required for email download. | Invalid attachment_id: required for resource='email' |
| Existing files are not replaced unless overwrite=true. | Invalid save_path: file exists. Expected: overwrite=true or another path |
| Folders cannot be downloaded. | Invalid id: item is a folder. Expected: a file |

### Example — Export a contact

```json
{
  "arguments": {
    "resource": "contact",
    "id": "AQMkADAwATM3ZmYAZS1kCON",
    "mode": "vcard"
  },
  "structuredContent": {
    "resource": "contact",
    "id": "AQMkADAwATM3ZmYAZS1kCON",
    "mode": "vcard",
    "saved_path": null,
    "size": null,
    "mime_type": "text/vcard",
    "download_url": null,
    "vcard": "BEGIN:VCARD\r\nVERSION:3.0\r\nFN:Jane Citizen\r\nEND:VCARD",
    "summary": "Exported Jane Citizen as vCard."
  }
}
```

<a id="m365_create"></a>

## `m365_create` — Create Item

Create a mail folder, calendar, contact, contact folder or OneDrive folder. Supply the sub-object that matches resource. Not for emails (email_create_draft), events (calendar_create_event), inbox rules (email_rule_manage) or uploading files (drive_upload). Returns the created item.

- **Tier:** core · **Category:** m365 · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `email_folder` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` | yes | — | — | Type of item to create. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `email_folder` | object | no | — | — | Required when resource='email_folder'. |
| `email_folder.display_name` | string | yes | — | min length 1, max length 255 | Folder name. |
| `email_folder.parent_id` | string | no | — | min length 1, max length 1024 | Parent mail folder ID or alias (default: top level). |
| `calendar` | object | no | — | — | Required when resource='calendar'. |
| `calendar.name` | string | yes | — | min length 1, max length 255 | Calendar name. |
| `contact` | object | no | — | — | Required when resource='contact'. |
| `contact.given_name` | string | yes | — | min length 1, max length 255 | First name. |
| `contact.surname` | string | no | — | max length 255 | Last name. |
| `contact.display_name` | string | no | — | max length 255 | Name shown in Outlook. Defaults to given_name + surname. |
| `contact.emails` | array of string (email) | no | — | max items 3, unique | Email addresses (at most 3). |
| `contact.mobile_phone` | string | no | — | max length 64 | Mobile phone number. |
| `contact.business_phones` | array of string | no | — | max items 5 | Business phone numbers. |
| `contact.home_phones` | array of string | no | — | max items 5 | Home phone numbers. |
| `contact.company_name` | string | no | — | max length 255 | Company. |
| `contact.job_title` | string | no | — | max length 255 | Job title. |
| `contact.department` | string | no | — | max length 255 | Department. |
| `contact.folder_id` | string | no | — | min length 1, max length 1024 | Contact folder ID (default: your main Contacts). |
| `contact_folder` | object | no | — | — | Required when resource='contact_folder'. |
| `contact_folder.display_name` | string | yes | — | min length 1, max length 255 | Folder name. |
| `contact_folder.parent_id` | string | no | — | min length 1, max length 1024 | Parent contact folder ID (default: top level). |
| `drive_folder` | object | no | — | — | Required when resource='drive_item' (creates a folder). |
| `drive_folder.name` | string | yes | — | min length 1, max length 255 | Folder name. |
| `drive_folder.parent_id` | string | no | — | min length 1, max length 1024 | Parent folder ID (default: OneDrive root). |
| `drive_folder.parent_path` | string | no | — | min length 1, max length 2048 | Parent folder path instead of parent_id, e.g. /Documents. |
| `drive_folder.if_exists` | `fail` \| `rename` | no | `"fail"` | — | What to do if a folder with this name exists. |

### Output

The created item.

| Field | Type |
|---|---|
| `resource` | `email_folder` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` |
| `item` | [email_folder](#type-email_folder) or [calendar](#type-calendar) or [contact](#type-contact) or [contact_folder](#type-contact_folder) or [drive_item](#type-drive_item) |
| `summary` | string |

### Microsoft Graph calls

- email_folder: POST /me/mailFolders or /me/mailFolders/{parent}/childFolders
- calendar: POST /me/calendars
- contact: POST /me/contacts or /me/contactFolders/{id}/contacts
- contact_folder: POST /me/contactFolders or /me/contactFolders/{parent}/childFolders
- drive_item: POST /me/drive/items/{parent}/children with folder facet and @microsoft.graph.conflictBehavior

### Server-side validation

| Rule | Error returned |
|---|---|
| Exactly one sub-object, and it must match resource. | Invalid contact: resource is 'calendar'. Expected: supply only the 'calendar' object |
| drive_folder.parent_id and parent_path are mutually exclusive. | Invalid parent_path: cannot be combined with parent_id |
| Name conflicts fail unless if_exists='rename'. | A folder named 'Tax' already exists there. Expected: another name or if_exists='rename' |

### Example — Create a contact in a folder

```json
{
  "arguments": {
    "resource": "contact",
    "contact": {
      "given_name": "Jane",
      "surname": "Citizen",
      "emails": [
        "jane@example.com"
      ],
      "mobile_phone": "+61 400 000 000",
      "folder_id": "AQMkADAwATM3ZmYAZS1kCFLD"
    }
  },
  "structuredContent": {
    "resource": "contact",
    "item": {
      "id": "AQMkADAwATM3ZmYAZS1kCON",
      "display_name": "Jane Citizen",
      "given_name": "Jane",
      "surname": "Citizen",
      "emails": [
        "jane@example.com"
      ],
      "mobile_phone": "+61 400 000 000",
      "business_phones": [],
      "home_phones": [],
      "company_name": null,
      "job_title": null,
      "department": null,
      "folder_id": "AQMkADAwATM3ZmYAZS1kCFLD"
    },
    "summary": "Created contact Jane Citizen."
  }
}
```

<a id="m365_update"></a>

## `m365_update` — Update Item

Change properties of an existing email, mail folder, contact or OneDrive item: mark read or unread, flag, set importance or categories, Focused/Other, rename folders and files, or edit contact details. Supply the *_changes object that matches resource. Not for events (calendar_update_event), inbox rules (email_rule_manage) or file contents (drive_upload). Returns the changed field names.

- **Tier:** core · **Category:** m365 · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `email` \| `email_folder` \| `contact` \| `drive_item` | yes | — | — | Type of item. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `id` | string | yes | — | min length 1, max length 1024 | Item ID. |
| `email_changes` | object | no | — | min fields 1 | Required when resource='email'. |
| `email_changes.is_read` | boolean | no | — | — | Mark read (true) or unread (false). |
| `email_changes.flag` | object | no | — | — | Follow-up flag. |
| `email_changes.flag.status` | `not_flagged` \| `flagged` \| `complete` | yes | — | — | Flag state. |
| `email_changes.flag.start_at` | string (date-time) | no | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `email_changes.flag.due_at` | string (date-time) | no | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `email_changes.importance` | `low` \| `normal` \| `high` | no | — | — | Message importance. |
| `email_changes.categories_add` | array of string | no | — | min items 1, max items 25, unique | Category names. |
| `email_changes.categories_remove` | array of string | no | — | min items 1, max items 25, unique | Category names. |
| `email_changes.categories_set` | array of string | no | — | min items 0, max items 25, unique | Replace all categories (empty list clears them). |
| `email_changes.inference_classification` | `focused` \| `other` | no | — | — | Focused Inbox placement. |
| `email_folder_changes` | object | no | — | — | Required when resource='email_folder'. |
| `email_folder_changes.display_name` | string | yes | — | min length 1, max length 255 | New folder name. |
| `contact_changes` | object | no | — | min fields 1 | Required when resource='contact'. Only supplied fields change; lists replace the existing list. |
| `contact_changes.given_name` | string | no | — | min length 1, max length 255 | First name. |
| `contact_changes.surname` | string | no | — | max length 255 | Last name. |
| `contact_changes.display_name` | string | no | — | max length 255 | Name shown in Outlook. Defaults to given_name + surname. |
| `contact_changes.emails` | array of string (email) | no | — | max items 3, unique | Email addresses (at most 3). |
| `contact_changes.mobile_phone` | string | no | — | max length 64 | Mobile phone number. |
| `contact_changes.business_phones` | array of string | no | — | max items 5 | Business phone numbers. |
| `contact_changes.home_phones` | array of string | no | — | max items 5 | Home phone numbers. |
| `contact_changes.company_name` | string | no | — | max length 255 | Company. |
| `contact_changes.job_title` | string | no | — | max length 255 | Job title. |
| `contact_changes.department` | string | no | — | max length 255 | Department. |
| `drive_item_changes` | object | no | — | — | Required when resource='drive_item'. |
| `drive_item_changes.name` | string | yes | — | min length 1, max length 255 | New name, including extension. |

### Output

Which fields changed.

| Field | Type |
|---|---|
| `resource` | `email` \| `email_folder` \| `contact` \| `drive_item` |
| `id` | string |
| `status` | any |
| `changed_fields` | array of string |
| `summary` | string |

### Microsoft Graph calls

- email: PATCH /me/messages/{id} (isRead, flag, importance, categories, inferenceClassification; add/remove reads current categories first)
- email_folder: PATCH /me/mailFolders/{id}
- contact: PATCH /me/contacts/{id}
- drive_item: PATCH /me/drive/items/{id} {name}

### Server-side validation

| Rule | Error returned |
|---|---|
| Exactly one *_changes object, matching resource. | Invalid email_changes: resource is 'contact'. Expected: contact_changes |
| categories_set cannot be combined with categories_add or categories_remove. | Invalid categories_set: cannot be combined with categories_add or categories_remove |
| Well-known mail folders cannot be renamed. | Invalid id: inbox cannot be renamed |
| The OneDrive root cannot be renamed. | Invalid id: the OneDrive root cannot be renamed |

### Example — Mark read and add a category

```json
{
  "arguments": {
    "resource": "email",
    "id": "AQMkADAwATM3ZmYAZS1k",
    "email_changes": {
      "is_read": true,
      "categories_add": [
        "Bills"
      ]
    }
  },
  "structuredContent": {
    "resource": "email",
    "id": "AQMkADAwATM3ZmYAZS1k",
    "status": "updated",
    "changed_fields": [
      "is_read",
      "categories"
    ],
    "summary": "Updated email: is_read, categories."
  }
}
```

<a id="m365_move"></a>

## `m365_move` — Move Item

Move an email, mail folder, contact or OneDrive item to another folder. Moving an email to 'archive' archives it; moving a contact places it in a contact folder. Moved emails and contacts get a new ID, which is returned: use it for later calls.

- **Tier:** core · **Category:** m365 · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `email` \| `email_folder` \| `contact` \| `drive_item` | yes | — | — | Type of item. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `id` | string | yes | — | min length 1, max length 1024 | Item to move. |
| `destination_id` | string | no | — | min length 1, max length 1024 | Destination folder. email: mail folder ID or alias (archive, inbox, junk, deleted, drafts, sent). email_folder: mail folder ID or 'root'. contact: contact folder ID or 'default'. drive_item: folder ID or 'root'. |
| `destination_path` | string | no | — | min length 1, max length 2048 | drive_item only: destination folder path instead of destination_id. |
| `new_name` | string | no | — | min length 1, max length 255 | drive_item only: rename while moving. |

### Output

Move result. status 'copied_not_removed' (contacts only) means the copy exists but deleting the original failed; both IDs are valid.

| Field | Type |
|---|---|
| `resource` | `email` \| `email_folder` \| `contact` \| `drive_item` |
| `previous_id` | string |
| `id` | string |
| `status` | `moved` \| `copied_not_removed` |
| `destination_id` | string |
| `summary` | string |

### Microsoft Graph calls

- email: POST /me/messages/{id}/move {destinationId} (returns new id)
- email_folder: POST /me/mailFolders/{id}/move {destinationId}
- contact: POST /me/contactFolders/{dest}/contacts (copy of fields) then DELETE /me/contacts/{id}
- drive_item: PATCH /me/drive/items/{id} {parentReference, name?}

### Server-side validation

| Rule | Error returned |
|---|---|
| Exactly one of destination_id or destination_path (path only for drive_item). | Invalid destination_id: required |
| A folder cannot be moved into itself or its own subfolder. | Invalid destination_id: is inside the folder being moved |
| Not retried after an ambiguous failure; the error says to check the destination first. | Outcome unknown: check the destination folder before retrying |

### Example — Archive an email

```json
{
  "arguments": {
    "resource": "email",
    "id": "AQMkADAwATM3ZmYAZS1k",
    "destination_id": "archive"
  },
  "structuredContent": {
    "resource": "email",
    "previous_id": "AQMkADAwATM3ZmYAZS1k",
    "id": "AQMkADAwATM3ZmYAZS2m",
    "status": "moved",
    "destination_id": "AQMkADAwATM3ZmYAZS1kARCH",
    "summary": "Moved email to Archive (new id AQMkADAwATM3ZmYAZS2m)."
  }
}
```

<a id="m365_delete"></a>

## `m365_delete` — Delete Item

Delete one email, mail folder, inbox rule, event, calendar, contact, contact folder or OneDrive item. Always requires confirm=true after the user approves. Deleting a meeting you organise sends cancellations to its attendees. OneDrive items go to the recycle bin. Returns what happened and whether it can be recovered.

- **Tier:** core · **Category:** m365 · **Safety:** critical
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=true, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `resource` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` | yes | — | — | Type of item. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `id` | string | yes | — | min length 1, max length 1024 | Item ID. Aliases are not accepted. |
| `cancellation_message` | string | no | — | max length 2000 | event only: note sent with the cancellation when you organise the meeting. |
| `confirm` | boolean | yes | `false` | — | Must be true to delete this item; set only after the user approves. |

### Output

Delete result. recoverable is true only where Graph documents recovery (OneDrive recycle bin).

| Field | Type |
|---|---|
| `resource` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` |
| `id` | string |
| `status` | `deleted` \| `cancelled_and_deleted` |
| `recoverable` | boolean |
| `summary` | string |

### Microsoft Graph calls

- email: DELETE /me/messages/{id}
- email_folder: DELETE /me/mailFolders/{id}
- email_rule: DELETE /me/mailFolders/inbox/messageRules/{id}
- event: organiser with attendees: POST /me/events/{id}/cancel {comment}; otherwise DELETE /me/events/{id}
- calendar: DELETE /me/calendars/{id}
- contact: DELETE /me/contacts/{id}
- contact_folder: DELETE /me/contactFolders/{id}
- drive_item: DELETE /me/drive/items/{id} (recycle bin)

### Server-side validation

| Rule | Error returned |
|---|---|
| confirm must be true. | Invalid confirm 'False': delete requires confirm=True to proceed. Expected: Explicit user confirmation |
| Aliases and protected items are refused: well-known mail folders, the default calendar, the OneDrive root. | Invalid id: the default calendar cannot be deleted |
| cancellation_message only for resource='event'. | Invalid cancellation_message: only valid for resource='event' |
| Never retried after an ambiguous failure. | Outcome unknown: check whether the item still exists before retrying |

### Example — Delete a OneDrive file

```json
{
  "arguments": {
    "resource": "drive_item",
    "id": "01ABCDEF2345",
    "confirm": true
  },
  "structuredContent": {
    "resource": "drive_item",
    "id": "01ABCDEF2345",
    "status": "deleted",
    "recoverable": true,
    "summary": "Moved 'Tax return 2026.pdf' to the OneDrive recycle bin."
  }
}
```

<a id="email_create_draft"></a>

## `email_create_draft` — Create Email Draft

Create an unsent email draft to review, then send later with email_send(mode='draft'). Sends nothing. Returns the draft ID.

- **Tier:** core · **Category:** email · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `to` | array of string (email) | yes | — | min items 1, max items 500, unique | Recipients (To). |
| `cc` | array of string (email) | no | — | max items 500, unique | Cc recipients. |
| `bcc` | array of string (email) | no | — | max items 500, unique | Bcc recipients. |
| `subject` | string | yes | — | min length 1, max length 998 | Subject line. |
| `body` | string | yes | — | max length 1000000 | Message body. |
| `body_format` | `text` \| `html` | no | `"text"` | — | Format of the body text you supply. |
| `attachments` | array of string | no | — | max items 10, unique | Local files to attach: at most 10, each at most 25 MB. |
| `importance` | `low` \| `normal` \| `high` | no | — | — | Message importance. |

### Output

The created draft.

| Field | Type |
|---|---|
| `draft_id` | string |
| `subject` | string |
| `to` | array of string |
| `cc` | array of string |
| `bcc` | array of string |
| `attachment_count` | integer |
| `web_link` | string or null |
| `summary` | string |

### Microsoft Graph calls

- POST /me/messages (draft)
- attachments < 3 MB: POST /me/messages/{id}/attachments
- attachments ≥ 3 MB: POST /me/messages/{id}/attachments/createUploadSession then chunked PUT (no Authorization header)

### Server-side validation

| Rule | Error returned |
|---|---|
| At most 500 unique recipients across to, cc and bcc. | Invalid to: more than 500 recipients in total |
| Attachments: at most 10 files, 25 MB each, inside allowed roots. | Invalid attachments: 'report.zip' is 31 MB. Expected: at most 25 MB |

### Example — Draft a note

```json
{
  "arguments": {
    "to": [
      "jane@example.com"
    ],
    "subject": "Weekend",
    "body": "Are you free Saturday?"
  },
  "structuredContent": {
    "draft_id": "AQMkADAwATM3ZmYAZS1kDRFT",
    "subject": "Weekend",
    "to": [
      "jane@example.com"
    ],
    "cc": [],
    "bcc": [],
    "attachment_count": 0,
    "web_link": "https://outlook.live.com/owa/?ItemID=AQMkADAwATM3ZmYAZS1kDRFT",
    "summary": "Draft 'Weekend' created for jane@example.com (not sent)."
  }
}
```

<a id="email_send"></a>

## `email_send` — Send Email

Send an email now: a new message (mode='new') or a draft you created earlier (mode='draft'). Requires confirm=true after the user approves the recipients and content. Cannot be undone. Never infer recipients.

- **Tier:** core · **Category:** email · **Safety:** dangerous
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `mode` | `new` \| `draft` | yes | — | — | Send a new message or an existing draft. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `to` | array of string (email) | no | — | min items 1, max items 500, unique | Recipients (To). |
| `cc` | array of string (email) | no | — | max items 500, unique | Cc recipients. |
| `bcc` | array of string (email) | no | — | max items 500, unique | Bcc recipients. |
| `subject` | string | no | — | min length 1, max length 998 | Subject line. |
| `body` | string | no | — | max length 1000000 | Message body. |
| `body_format` | `text` \| `html` | no | `"text"` | — | Format of the body text you supply. |
| `attachments` | array of string | no | — | max items 10, unique | Local files to attach: at most 10, each at most 25 MB. |
| `importance` | `low` \| `normal` \| `high` | no | — | — | Message importance. |
| `save_to_sent` | boolean | no | `true` | — | mode='new': keep a copy in Sent Items. |
| `draft_id` | string | no | — | min length 1, max length 1024 | mode='draft': draft ID from email_create_draft. |
| `confirm` | boolean | yes | `false` | — | Must be true to send this email; set only after the user approves. |

### Output

Send confirmation.

| Field | Type |
|---|---|
| `status` | any |
| `mode` | `new` \| `draft` |
| `draft_id` | string or null |
| `recipient_count` | integer |
| `sent_at` | string (date-time) |
| `summary` | string |

### Microsoft Graph calls

- new without attachments ≥ 3 MB: POST /me/sendMail {message, saveToSentItems}
- new with large attachments: POST /me/messages → upload sessions → POST /me/messages/{id}/send
- draft: POST /me/messages/{draft_id}/send

### Server-side validation

| Rule | Error returned |
|---|---|
| mode='new' requires to, subject and body; mode='draft' requires draft_id and forbids message fields. | Invalid draft_id: required when mode='draft' |
| confirm must be true. | Invalid confirm 'False': send email requires confirm=True to proceed. Expected: Explicit user confirmation |
| Never retried after a timeout or ambiguous 5xx. | Outcome unknown: check Sent Items before retrying |

### Example — Send a reviewed draft

```json
{
  "arguments": {
    "mode": "draft",
    "draft_id": "AQMkADAwATM3ZmYAZS1kDRFT",
    "confirm": true
  },
  "structuredContent": {
    "status": "sent",
    "mode": "draft",
    "draft_id": "AQMkADAwATM3ZmYAZS1kDRFT",
    "recipient_count": 1,
    "sent_at": "2026-09-26T10:05:00+09:30",
    "summary": "Sent 'Weekend' to 1 recipient."
  }
}
```

<a id="email_reply"></a>

## `email_reply` — Reply to Email

Reply to an email, either to the sender only (mode='sender') or to everyone on it (mode='all'). Requires confirm=true after the user approves the reply. Returns the conversation the reply joined.

- **Tier:** core · **Category:** email · **Safety:** dangerous
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `email_id` | string | yes | — | min length 1, max length 1024 | Email to reply to. |
| `mode` | `sender` \| `all` | yes | — | — | sender: reply to the sender. all: reply all. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `body` | string | yes | — | min length 1, max length 1000000 | Reply text. |
| `body_format` | `text` \| `html` | no | `"text"` | — | Format of the body text you supply. |
| `cc` | array of string (email) | no | — | max items 500, unique | Extra Cc recipients. |
| `attachments` | array of string | no | — | max items 10, unique | Local files to attach: at most 10, each at most 25 MB. |
| `confirm` | boolean | yes | `false` | — | Must be true to send this reply; set only after the user approves. |

### Output

Reply confirmation.

| Field | Type |
|---|---|
| `status` | any |
| `mode` | `sender` \| `all` |
| `in_reply_to` | string |
| `conversation_id` | string or null |
| `summary` | string |

### Microsoft Graph calls

- plain: POST /me/messages/{id}/reply or /replyAll {comment}
- with cc/attachments: POST /createReply or /createReplyAll → PATCH → attachments → POST /send

### Server-side validation

| Rule | Error returned |
|---|---|
| confirm must be true. | Invalid confirm 'False': reply requires confirm=True to proceed. Expected: Explicit user confirmation |
| Never retried after an ambiguous failure. | Outcome unknown: check Sent Items before retrying |

### Example — Reply all

```json
{
  "arguments": {
    "email_id": "AQMkADAwATM3ZmYAZS1k",
    "mode": "all",
    "body": "Tuesday works for me.",
    "confirm": true
  },
  "structuredContent": {
    "status": "sent",
    "mode": "all",
    "in_reply_to": "AQMkADAwATM3ZmYAZS1k",
    "conversation_id": "AQQkADAwATM3ZmYAZS1",
    "summary": "Replied to all on 'Planning meeting'."
  }
}
```

<a id="email_forward"></a>

## `email_forward` — Forward Email

Forward an email to recipients the user names, with an optional note. Requires confirm=true after the user approves. Never infer recipients from other context.

- **Tier:** core · **Category:** email · **Safety:** dangerous
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `email_id` | string | yes | — | min length 1, max length 1024 | Email to forward. |
| `to` | array of string (email) | yes | — | min items 1, max items 500, unique | Recipients. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `cc` | array of string (email) | no | — | max items 500, unique | Cc recipients. |
| `bcc` | array of string (email) | no | — | max items 500, unique | Bcc recipients. |
| `comment` | string | no | — | max length 100000 | Note above the forwarded message. |
| `attachments` | array of string | no | — | max items 10, unique | Local files to attach: at most 10, each at most 25 MB. |
| `confirm` | boolean | yes | `false` | — | Must be true to forward this email; set only after the user approves. |

### Output

Forward confirmation.

| Field | Type |
|---|---|
| `status` | any |
| `forwarded_id` | string |
| `recipient_count` | integer |
| `summary` | string |

### Microsoft Graph calls

- plain: POST /me/messages/{id}/forward {toRecipients, comment}
- with cc/bcc/attachments: POST /createForward → PATCH → attachments → POST /send

### Server-side validation

| Rule | Error returned |
|---|---|
| confirm must be true. | Invalid confirm 'False': forward requires confirm=True to proceed. Expected: Explicit user confirmation |
| Never retried after an ambiguous failure. | Outcome unknown: check Sent Items before retrying |

### Example — Forward with a note

```json
{
  "arguments": {
    "email_id": "AQMkADAwATM3ZmYAZS1k",
    "to": [
      "accountant@example.com"
    ],
    "comment": "For our records.",
    "confirm": true
  },
  "structuredContent": {
    "status": "sent",
    "forwarded_id": "AQMkADAwATM3ZmYAZS1k",
    "recipient_count": 1,
    "summary": "Forwarded 'Your September invoice' to 1 recipient."
  }
}
```

<a id="calendar_create_event"></a>

## `calendar_create_event` — Create Event

Add an event to your calendar. With attendees, Outlook emails them an invitation, so confirm=true is then required. Without attendees it is a private appointment and needs no confirmation. Returns the event.

- **Tier:** core · **Category:** calendar · **Safety:** dangerous
- **Confirm:** conditional — Required when attendees is non-empty (Outlook emails each attendee an invitation).
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `subject` | string | yes | — | min length 1, max length 255 | Title. |
| `start` | string (date-time) | yes | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `end` | string (date-time) | yes | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `time_zone` | string | no | — | min length 1, max length 64 | IANA time-zone name, e.g. Australia/Adelaide. Defaults to the mailbox time zone. |
| `is_all_day` | boolean | no | — | — | All-day event (start and end at midnight in time_zone). |
| `location` | string | no | — | max length 255 | Location text. |
| `body` | string | no | — | max length 100000 | Description. |
| `body_format` | `text` \| `html` | no | `"text"` | — | Format of the body text you supply. |
| `reminder_minutes` | integer | no | — | min 0, max 40320 | Reminder before start, in minutes. |
| `show_as` | `free` \| `tentative` \| `busy` \| `oof` \| `workingElsewhere` | no | — | — | How the time shows in your calendar. |
| `attendees` | array of object | no | — | max items 500 | People to invite. |
| `calendar_id` | string | no | — | min length 1, max length 1024 | Calendar ID (default: your default calendar). |
| `confirm` | boolean | no | `false` | — | Must be true to send invitations to the attendees; set only after the user approves. |

### Output

The created event.

| Field | Type |
|---|---|
| `event` | [event_detail](#type-event_detail) |
| `invitations_sent` | boolean |
| `summary` | string |

### Microsoft Graph calls

- POST /me/events or /me/calendars/{id}/events (Prefer: outlook.timezone)

### Server-side validation

| Rule | Error returned |
|---|---|
| end must be after start. | Invalid end: must be after start |
| confirm required when attendees is non-empty. | Invalid confirm 'False': inviting attendees requires confirm=True to proceed. Expected: Explicit user confirmation |
| Never retried after an ambiguous failure. | Outcome unknown: check your calendar before retrying |

### Example — Private appointment

```json
{
  "arguments": {
    "subject": "Dentist",
    "start": "2026-10-02T09:00:00+09:30",
    "end": "2026-10-02T10:00:00+09:30",
    "location": "City Dental"
  },
  "structuredContent": {
    "event": {
      "id": "AAMkADAwATM3EVT",
      "subject": "Dentist",
      "start": "2026-10-02T09:00:00+09:30",
      "end": "2026-10-02T10:00:00+09:30",
      "time_zone": "Australia/Adelaide",
      "is_all_day": false,
      "location": "City Dental",
      "organizer": {
        "name": "Robin",
        "address": "robin@example.com"
      },
      "is_organizer": true,
      "my_response": "organizer",
      "show_as": "busy",
      "attendee_count": 0,
      "calendar_id": "AAMkADAwATM3CAL",
      "preview": null,
      "body": null,
      "body_truncated": false,
      "attendees": [],
      "web_link": "https://outlook.live.com/calendar/item/AAMkADAwATM3EVT",
      "recurrence": null
    },
    "invitations_sent": false,
    "summary": "Created 'Dentist' on 2 Oct 09:00–10:00 (no invitations sent)."
  }
}
```

<a id="calendar_update_event"></a>

## `calendar_update_event` — Update Event

Change an event's time, title, location, description, reminder or attendees. If the event has attendees, or you add some, Outlook emails them an update, so confirm=true is then required. Returns the updated event.

- **Tier:** core · **Category:** calendar · **Safety:** dangerous
- **Confirm:** conditional — Required when the event has attendees or the change adds any (Outlook emails them an update).
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `event_id` | string | yes | — | min length 1, max length 1024 | Event to change. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `changes` | object | yes | — | min fields 1 | Fields to change (at least one). |
| `changes.subject` | string | no | — | min length 1, max length 255 | Title. |
| `changes.start` | string (date-time) | no | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `changes.end` | string (date-time) | no | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `changes.time_zone` | string | no | — | min length 1, max length 64 | IANA time-zone name, e.g. Australia/Adelaide. Defaults to the mailbox time zone. |
| `changes.is_all_day` | boolean | no | — | — | All-day event (start and end at midnight in time_zone). |
| `changes.location` | string | no | — | max length 255 | Location text. |
| `changes.body` | string | no | — | max length 100000 | Description. |
| `changes.body_format` | `text` \| `html` | no | `"text"` | — | Format of the body text you supply. |
| `changes.reminder_minutes` | integer | no | — | min 0, max 40320 | Reminder before start, in minutes. |
| `changes.show_as` | `free` \| `tentative` \| `busy` \| `oof` \| `workingElsewhere` | no | — | — | How the time shows in your calendar. |
| `changes.attendees_set` | array of object | no | — | max items 500 | Replace the attendee list. |
| `changes.attendees_add` | array of object | no | — | min items 1, max items 500 | Attendees to add. |
| `changes.attendees_remove` | array of string (email) | no | — | min items 1, max items 500, unique | Attendee addresses to remove. |
| `confirm` | boolean | no | `false` | — | Must be true to send updates to the attendees; set only after the user approves. |

### Output

The updated event.

| Field | Type |
|---|---|
| `event` | [event_detail](#type-event_detail) |
| `attendees_notified` | boolean |
| `changed_fields` | array of string |
| `summary` | string |

### Microsoft Graph calls

- GET /me/events/{id} (to detect attendees)
- PATCH /me/events/{id}

### Server-side validation

| Rule | Error returned |
|---|---|
| attendees_set cannot be combined with attendees_add or attendees_remove. | Invalid attendees_set: cannot be combined with attendees_add or attendees_remove |
| Resulting end must be after start. | Invalid end: must be after start |
| Only the organiser can change time, attendees or location of a meeting. | You are not the organiser of this meeting. Expected: use calendar_respond to propose a new time |
| confirm required when attendees are affected. | Invalid confirm 'False': updating a meeting with attendees requires confirm=True to proceed. Expected: Explicit user confirmation |

### Example — Move a private appointment

```json
{
  "arguments": {
    "event_id": "AAMkADAwATM3EVT",
    "changes": {
      "start": "2026-10-02T11:00:00+09:30",
      "end": "2026-10-02T12:00:00+09:30"
    }
  },
  "structuredContent": {
    "event": {
      "id": "AAMkADAwATM3EVT",
      "subject": "Dentist",
      "start": "2026-10-02T11:00:00+09:30",
      "end": "2026-10-02T12:00:00+09:30",
      "time_zone": "Australia/Adelaide",
      "is_all_day": false,
      "location": "City Dental",
      "organizer": {
        "name": "Robin",
        "address": "robin@example.com"
      },
      "is_organizer": true,
      "my_response": "organizer",
      "show_as": "busy",
      "attendee_count": 0,
      "calendar_id": "AAMkADAwATM3CAL",
      "preview": null,
      "body": null,
      "body_truncated": false,
      "attendees": [],
      "web_link": "https://outlook.live.com/calendar/item/AAMkADAwATM3EVT",
      "recurrence": null
    },
    "attendees_notified": false,
    "changed_fields": [
      "start",
      "end"
    ],
    "summary": "Moved 'Dentist' to 2 Oct 11:00–12:00."
  }
}
```

<a id="calendar_respond"></a>

## `calendar_respond` — Respond to Invitation

Accept, tentatively accept or decline a meeting invitation, optionally proposing a new time (with tentative or decline). When a response is emailed to the organiser (the default), confirm=true is required.

- **Tier:** core · **Category:** calendar · **Safety:** dangerous
- **Confirm:** conditional — Required when send_response is true (the organiser is emailed).
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `event_id` | string | yes | — | min length 1, max length 1024 | Invitation's event ID. |
| `action` | `accept` \| `tentative` \| `decline` | yes | — | — | Your response. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `comment` | string | no | — | max length 2000 | Message to the organiser. |
| `send_response` | boolean | no | `true` | — | Email the response to the organiser. |
| `proposed_start` | string (date-time) | no | — | — | Proposed new start (tentative or decline only). |
| `proposed_end` | string (date-time) | no | — | — | Proposed new end (tentative or decline only). |
| `confirm` | boolean | no | `false` | — | Must be true to send this response to the organiser; set only after the user approves. |

### Output

Response result.

| Field | Type |
|---|---|
| `event_id` | string |
| `action` | `accept` \| `tentative` \| `decline` |
| `response_sent` | boolean |
| `proposed_start` | string (date-time) or null |
| `proposed_end` | string (date-time) or null |
| `summary` | string |

### Microsoft Graph calls

- POST /me/events/{id}/accept | /tentativelyAccept | /decline {comment, sendResponse, proposedNewTime?}

### Server-side validation

| Rule | Error returned |
|---|---|
| proposed_start and proposed_end come together, only with tentative or decline, end after start. | Invalid proposed_end: required with proposed_start |
| You cannot respond to your own meeting. | You organise this meeting; there is nothing to respond to |
| confirm required when send_response is true. | Invalid confirm 'False': sending a response requires confirm=True to proceed. Expected: Explicit user confirmation |

### Example — Decline and propose another time

```json
{
  "arguments": {
    "event_id": "AAMkADAwATM3INV",
    "action": "decline",
    "comment": "Can we do Thursday?",
    "proposed_start": "2026-10-08T14:00:00+09:30",
    "proposed_end": "2026-10-08T15:00:00+09:30",
    "confirm": true
  },
  "structuredContent": {
    "event_id": "AAMkADAwATM3INV",
    "action": "decline",
    "response_sent": true,
    "proposed_start": "2026-10-08T14:00:00+09:30",
    "proposed_end": "2026-10-08T15:00:00+09:30",
    "summary": "Declined 'Book club' and proposed 8 Oct 14:00–15:00."
  }
}
```

<a id="calendar_find_availability"></a>

## `calendar_find_availability` — Find Free Time

Show when you are busy in your own calendar during a time range and, given slot_minutes, suggest free slots of that length within your working hours. Personal accounts cannot see other people's availability, so this covers your calendar only.

- **Tier:** core · **Category:** calendar · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `start` | string (date-time) | yes | — | — | RFC 3339 date-time with offset, e.g. 2026-10-01T09:00:00+09:30. |
| `end` | string (date-time) | yes | — | — | Range end; at most 62 days after start. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `time_zone` | string | no | — | min length 1, max length 64 | IANA time-zone name, e.g. Australia/Adelaide. Defaults to the mailbox time zone. |
| `slot_minutes` | integer | no | — | min 15, max 480 | Length of free slots to suggest. |
| `working_hours_only` | boolean | no | `true` | — | Only suggest slots inside your Outlook working hours. |
| `min_gap_minutes` | integer | no | `0` | min 0, max 120 | Buffer to keep before and after busy time. |
| `max_slots` | integer | no | `10` | min 1, max 50 | Most free slots to return. |

### Output

Busy blocks and suggested free slots.

| Field | Type |
|---|---|
| `time_zone` | string |
| `busy` | array of object |
| `free_slots` | array of object |
| `working_hours` | object or null |
| `summary` | string |

### Microsoft Graph calls

- GET /me/calendarView?startDateTime&endDateTime&$select=subject,start,end,showAs (paged)
- GET /me/mailboxSettings/workingHours
- Free slots computed server-side (getSchedule/findMeetingTimes are not used: undocumented or unsupported for personal accounts)

### Server-side validation

| Rule | Error returned |
|---|---|
| end after start, at most 62 days. | Invalid end: must be after start and within 62 days |
| working_hours_only has no effect without slot_minutes. | (no error; busy blocks are always returned) |

### Example — An hour free on Thursday

```json
{
  "arguments": {
    "start": "2026-10-01T00:00:00+09:30",
    "end": "2026-10-02T00:00:00+09:30",
    "slot_minutes": 60,
    "max_slots": 1
  },
  "structuredContent": {
    "time_zone": "Australia/Adelaide",
    "busy": [
      {
        "start": "2026-10-01T09:00:00+09:30",
        "end": "2026-10-01T10:30:00+09:30",
        "show_as": "busy",
        "subject": "Physio"
      }
    ],
    "free_slots": [
      {
        "start": "2026-10-01T10:30:00+09:30",
        "end": "2026-10-01T11:30:00+09:30"
      }
    ],
    "working_hours": {
      "days": [
        "monday",
        "tuesday",
        "wednesday",
        "thursday",
        "friday"
      ],
      "start_time": "08:00:00",
      "end_time": "17:00:00",
      "time_zone": "Australia/Adelaide"
    },
    "summary": "1 busy block; first free hour 10:30–11:30."
  }
}
```

<a id="drive_upload"></a>

## `drive_upload` — Upload File

Upload a local file to OneDrive, as a new file (parent_id or parent_path) or by replacing an existing file's contents (item_id). Reads from the local disk, inside the allowed folders only. Returns the OneDrive item.

- **Tier:** extended · **Category:** drive · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `local_path` | string | yes | — | min length 1, max length 4096 | Local file path inside the server's allowed folders. Hidden and secret files are refused. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `parent_id` | string | no | — | min length 1, max length 1024 | New file: destination folder ID ('root' allowed). |
| `parent_path` | string | no | — | min length 1, max length 2048 | New file: destination folder path, e.g. /Documents. |
| `name` | string | no | — | min length 1, max length 255 | New file: name in OneDrive (default: local file name). |
| `item_id` | string | no | — | min length 1, max length 1024 | Replace the contents of this existing file. |
| `if_exists` | `fail` \| `replace` \| `rename` | no | `"fail"` | — | New file: what to do if the name is taken. |

### Output

The uploaded file.

| Field | Type |
|---|---|
| `item` | [drive_item](#type-drive_item) |
| `status` | `created` \| `replaced` \| `renamed` |
| `summary` | string |

### Microsoft Graph calls

- ≤ 4 MB: PUT /me/drive/items/{parent}:/{name}:/content or /me/drive/items/{item}/content
- > 4 MB: POST …/createUploadSession then 15 × 320 KiB chunks (no Authorization header)

### Server-side validation

| Rule | Error returned |
|---|---|
| Exactly one target: item_id, or parent_id/parent_path (not both). | Invalid item_id: cannot be combined with parent_id or parent_path |
| local_path inside allowed roots, not deny-listed, and a regular file. | Invalid local_path: '.env' is a protected file |
| if_exists='fail' and the name exists. | A file named 'budget.xlsx' already exists there. Expected: if_exists='replace' or 'rename' |

### Example — Upload a new file

```json
{
  "arguments": {
    "local_path": "C:/Users/me/Downloads/budget.xlsx",
    "parent_path": "/Documents"
  },
  "structuredContent": {
    "item": {
      "id": "01ABCDEF6789",
      "name": "budget.xlsx",
      "item_type": "file",
      "path": "/Documents",
      "parent_id": "01ABCDEF0002",
      "size": 20480,
      "modified_at": "2026-09-26T10:10:00+09:30",
      "created_at": "2026-09-26T10:10:00+09:30",
      "mime_type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
      "child_count": null,
      "web_url": "https://onedrive.live.com/?id=01ABCDEF6789",
      "shared": false,
      "children": null
    },
    "status": "created",
    "summary": "Uploaded budget.xlsx to /Documents."
  }
}
```

<a id="drive_copy"></a>

## `drive_copy` — Copy File

Copy a OneDrive file or folder to another folder. Copying runs in the background: this returns an operation_id to check with m365_get(resource='operation').

- **Tier:** extended · **Category:** drive · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `item_id` | string | yes | — | min length 1, max length 1024 | File or folder to copy. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `destination_id` | string | no | — | min length 1, max length 1024 | Destination folder ID ('root' allowed). |
| `destination_path` | string | no | — | min length 1, max length 2048 | Destination folder path instead of destination_id. |
| `new_name` | string | no | — | min length 1, max length 255 | Name for the copy (default: same name). |

### Output

Started copy.

| Field | Type |
|---|---|
| `operation_id` | string |
| `status` | any |
| `summary` | string |

### Microsoft Graph calls

- POST /me/drive/items/{id}/copy {parentReference, name} → 202 + Location monitor URL (stored server-side for 24 hours)

### Server-side validation

| Rule | Error returned |
|---|---|
| Exactly one of destination_id or destination_path. | Invalid destination_path: cannot be combined with destination_id |

### Example — Copy a file

```json
{
  "arguments": {
    "item_id": "01ABCDEF6789",
    "destination_path": "/Backups"
  },
  "structuredContent": {
    "operation_id": "op_7f3c2a",
    "status": "in_progress",
    "summary": "Copy started; check with m365_get(resource='operation', id='op_7f3c2a')."
  }
}
```

<a id="drive_share"></a>

## `drive_share` — Share File

Share a OneDrive file or folder by link (mode='link') or by inviting named people (mode='invite'). A view or edit link works for anyone who has it. Always requires confirm=true after the user approves who gets access. Returns the link or the invitation results.

- **Tier:** extended · **Category:** drive · **Safety:** dangerous
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `item_id` | string | yes | — | min length 1, max length 1024 | File or folder to share (not the OneDrive root). |
| `mode` | `link` \| `invite` | yes | — | — | Share by link or invite specific people. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `link_type` | `view` \| `edit` \| `embed` | no | — | — | link: view (read-only), edit, or embed (files only). |
| `recipients` | array of string (email) | no | — | min items 1, max items 50, unique | invite: people to share with. |
| `role` | `read` \| `write` | no | — | — | invite: access level. |
| `message` | string | no | — | max length 2000 | invite: note in the invitation email. |
| `send_invitation` | boolean | no | `true` | — | invite: email the recipients. |
| `require_sign_in` | boolean | no | `true` | — | invite: recipients must sign in. |
| `password` | string | no | — | min length 4, max length 256 | Optional password for the link or invitation. |
| `expires_at` | string (date-time) | no | — | — | Optional expiry (invite expiry needs a premium OneDrive). |
| `confirm` | boolean | yes | `false` | — | Must be true to grant this access; set only after the user approves. |

### Output

Sharing result. created=false means an existing link of that type was returned.

| Field | Type |
|---|---|
| `mode` | `link` \| `invite` |
| `permission_ids` | array of string |
| `link_url` | string or null |
| `created` | boolean or null |
| `recipients` | array of object |
| `summary` | string |

### Microsoft Graph calls

- link: POST /me/drive/items/{id}/createLink {type, scope:'anonymous', password?, expirationDateTime?}
- invite: POST /me/drive/items/{id}/invite {recipients, roles, message, sendInvitation, requireSignIn, password?, expirationDateTime?} (207 partial success mapped per recipient)

### Server-side validation

| Rule | Error returned |
|---|---|
| link needs link_type; invite needs recipients and role; fields of the other mode are rejected. | Invalid recipients: only valid with mode='invite' |
| embed links only for files. | Invalid link_type 'embed': only files can be embedded |
| The OneDrive root cannot be shared. | Invalid item_id: the OneDrive root cannot be shared |
| confirm must be true. | Invalid confirm 'False': sharing requires confirm=True to proceed. Expected: Explicit user confirmation |

### Example — View-only link

```json
{
  "arguments": {
    "item_id": "01ABCDEF6789",
    "mode": "link",
    "link_type": "view",
    "confirm": true
  },
  "structuredContent": {
    "mode": "link",
    "permission_ids": [
      "aTowIy5mfG1lbWJlcnNoaXA"
    ],
    "link_url": "https://1drv.ms/x/s!AbCdEf",
    "created": true,
    "recipients": [],
    "summary": "Created a view-only link to budget.xlsx (anyone with the link can open it)."
  }
}
```

<a id="email_folder_mark_all_read"></a>

## `email_folder_mark_all_read` — Mark Folder Read

Mark every unread message in one mail folder as read, up to max_messages per call. If remaining_unread is above zero, call again. Use m365_update for a single message.

- **Tier:** extended · **Category:** email · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `folder_id` | string | yes | — | min length 1, max length 1024 | Mail folder ID, or one of the aliases inbox, sent, drafts, deleted, junk, archive. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `max_messages` | integer | no | `1000` | min 1, max 5000 | Most messages to change in this call. |

### Output

Progress.

| Field | Type |
|---|---|
| `folder_id` | string |
| `marked` | integer |
| `remaining_unread` | integer |
| `summary` | string |

### Microsoft Graph calls

- GET /me/mailFolders/{id}/messages?$filter=isRead eq false&$select=id (paged)
- POST /$batch (20 × PATCH /me/messages/{id} {isRead:true} per batch)

### Server-side validation

| Rule | Error returned |
|---|---|
| Unknown aliases rejected. | Invalid folder_id 'Inbx': unknown folder alias |

### Example — Mark junk read

```json
{
  "arguments": {
    "folder_id": "junk"
  },
  "structuredContent": {
    "folder_id": "AQMkADAwATM3ZmYAZS1kJUNK",
    "marked": 42,
    "remaining_unread": 0,
    "summary": "Marked 42 messages read in Junk Email."
  }
}
```

<a id="email_folder_empty"></a>

## `email_folder_empty` — Empty Mail Folder

Delete all messages in one mail folder, such as Junk Email or Deleted Items, up to max_messages per call. Subfolders are kept. Requires confirm=true after the user approves. Deleted messages cannot be restored with these tools.

- **Tier:** extended · **Category:** email · **Safety:** critical
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=true, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `folder_id` | string | yes | — | min length 1, max length 1024 | Mail folder ID, or one of the aliases inbox, sent, drafts, deleted, junk, archive. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `max_messages` | integer | no | `1000` | min 1, max 5000 | Most messages to delete in this call. |
| `confirm` | boolean | yes | `false` | — | Must be true to delete every message in this folder; set only after the user approves. |

### Output

Progress.

| Field | Type |
|---|---|
| `folder_id` | string |
| `deleted` | integer |
| `remaining` | integer |
| `summary` | string |

### Microsoft Graph calls

- GET /me/mailFolders/{id}/messages?$select=id (paged)
- POST /$batch (20 × DELETE /me/messages/{id} per batch)

### Server-side validation

| Rule | Error returned |
|---|---|
| confirm must be true. | Invalid confirm 'False': empty folder requires confirm=True to proceed. Expected: Explicit user confirmation |
| Partial batch failures are reported, not retried blindly. | Deleted 180 of 200; 20 failed (see remaining). Call again to retry the rest |

### Example — Empty Deleted Items

```json
{
  "arguments": {
    "folder_id": "deleted",
    "confirm": true
  },
  "structuredContent": {
    "folder_id": "AQMkADAwATM3ZmYAZS1kDEL",
    "deleted": 318,
    "remaining": 0,
    "summary": "Deleted 318 messages from Deleted Items."
  }
}
```

<a id="email_rule_manage"></a>

## `email_rule_manage` — Manage Inbox Rule

Create, change, enable or disable, or reorder an Inbox rule. Rules that forward, redirect or delete mail require confirm=true because they act silently on future mail. Read rules with m365_list or m365_get; delete one with m365_delete. Returns the rule.

- **Tier:** extended · **Category:** email · **Safety:** dangerous
- **Confirm:** conditional — Required when the resulting rule forwards, redirects or deletes mail (forward_to, forward_as_attachment_to, redirect_to, delete, permanent_delete).
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `action` | `create` \| `update` \| `set_enabled` \| `reorder` | yes | — | — | What to do. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `rule_id` | string | no | — | min length 1, max length 1024 | Existing rule (update, set_enabled, reorder). |
| `rule` | object | no | — | min fields 1 | Rule definition. create requires display_name, conditions and actions; update applies only the supplied fields. |
| `rule.display_name` | string | no | — | min length 1, max length 255 | Rule name. |
| `rule.conditions` | object | no | — | min fields 1 | Conditions that must all match. |
| `rule.conditions.subject_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.body_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.body_or_subject_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.sender_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.recipient_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.header_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.categories` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.conditions.from_addresses` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.conditions.sent_to_addresses` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.conditions.has_attachments` | boolean | no | — | — | Message has attachments. |
| `rule.conditions.importance` | `low` \| `normal` \| `high` | no | — | — | Message importance. |
| `rule.conditions.sensitivity` | `normal` \| `personal` \| `private` \| `confidential` | no | — | — | Message sensitivity. |
| `rule.conditions.message_action_flag` | `any` \| `call` \| `doNotForward` \| `followUp` \| `fyi` \| `forward` \| `noResponseNecessary` \| `read` \| `reply` \| `replyToAll` \| `review` | no | — | — | Flag-for-action value on the message. |
| `rule.conditions.within_size_range` | object | no | — | min fields 1 | Message size range in kilobytes. |
| `rule.conditions.within_size_range.minimum_kb` | integer | no | — | min 0, max 2147483647 | Minimum size in KB. |
| `rule.conditions.within_size_range.maximum_kb` | integer | no | — | min 0, max 2147483647 | Maximum size in KB. |
| `rule.conditions.is_approval_request` | boolean | no | — | — | Message is an approval request. |
| `rule.conditions.is_automatic_forward` | boolean | no | — | — | Message was auto-forwarded. |
| `rule.conditions.is_automatic_reply` | boolean | no | — | — | Message is an automatic reply. |
| `rule.conditions.is_encrypted` | boolean | no | — | — | Message is encrypted. |
| `rule.conditions.is_meeting_request` | boolean | no | — | — | Message is a meeting request. |
| `rule.conditions.is_meeting_response` | boolean | no | — | — | Message is a meeting response. |
| `rule.conditions.is_non_delivery_report` | boolean | no | — | — | Message is a non-delivery report. |
| `rule.conditions.is_permission_controlled` | boolean | no | — | — | Message is rights-protected. |
| `rule.conditions.is_read_receipt` | boolean | no | — | — | Message is a read receipt. |
| `rule.conditions.is_signed` | boolean | no | — | — | Message is S/MIME signed. |
| `rule.conditions.is_voicemail` | boolean | no | — | — | Message is a voicemail. |
| `rule.conditions.not_sent_to_me` | boolean | no | — | — | You are not a recipient. |
| `rule.conditions.sent_cc_me` | boolean | no | — | — | You are in Cc. |
| `rule.conditions.sent_only_to_me` | boolean | no | — | — | You are the only recipient. |
| `rule.conditions.sent_to_me` | boolean | no | — | — | You are in To. |
| `rule.conditions.sent_to_or_cc_me` | boolean | no | — | — | You are in To or Cc. |
| `rule.actions` | object | no | — | min fields 1 | Rule actions. Names map 1:1 to Graph messageRuleActions in camelCase. forward_to, forward_as_attachment_to, redirect_to, delete and permanent_delete require confirm=true. |
| `rule.actions.move_to_folder` | string | no | — | min length 1, max length 1024 | Mail folder ID to move the message to. |
| `rule.actions.copy_to_folder` | string | no | — | min length 1, max length 1024 | Mail folder ID to copy the message to. |
| `rule.actions.assign_categories` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.actions.mark_as_read` | boolean | no | — | — | Mark the message as read. |
| `rule.actions.mark_importance` | `low` \| `normal` \| `high` | no | — | — | Set importance. |
| `rule.actions.stop_processing_rules` | boolean | no | — | — | Do not evaluate later rules. |
| `rule.actions.forward_to` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.actions.forward_as_attachment_to` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.actions.redirect_to` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.actions.delete` | boolean | no | — | — | Move the message to Deleted Items. |
| `rule.actions.permanent_delete` | boolean | no | — | — | Delete the message permanently, skipping Deleted Items. |
| `rule.exceptions` | object | no | — | min fields 1 | Conditions that stop the rule applying. |
| `rule.exceptions.subject_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.body_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.body_or_subject_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.sender_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.recipient_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.header_contains` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.categories` | array of string | no | — | min items 1, max items 50 | Any of these strings. |
| `rule.exceptions.from_addresses` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.exceptions.sent_to_addresses` | array of string (email) | no | — | min items 1, max items 50 | Any of these addresses. |
| `rule.exceptions.has_attachments` | boolean | no | — | — | Message has attachments. |
| `rule.exceptions.importance` | `low` \| `normal` \| `high` | no | — | — | Message importance. |
| `rule.exceptions.sensitivity` | `normal` \| `personal` \| `private` \| `confidential` | no | — | — | Message sensitivity. |
| `rule.exceptions.message_action_flag` | `any` \| `call` \| `doNotForward` \| `followUp` \| `fyi` \| `forward` \| `noResponseNecessary` \| `read` \| `reply` \| `replyToAll` \| `review` | no | — | — | Flag-for-action value on the message. |
| `rule.exceptions.within_size_range` | object | no | — | min fields 1 | Message size range in kilobytes. |
| `rule.exceptions.within_size_range.minimum_kb` | integer | no | — | min 0, max 2147483647 | Minimum size in KB. |
| `rule.exceptions.within_size_range.maximum_kb` | integer | no | — | min 0, max 2147483647 | Maximum size in KB. |
| `rule.exceptions.is_approval_request` | boolean | no | — | — | Message is an approval request. |
| `rule.exceptions.is_automatic_forward` | boolean | no | — | — | Message was auto-forwarded. |
| `rule.exceptions.is_automatic_reply` | boolean | no | — | — | Message is an automatic reply. |
| `rule.exceptions.is_encrypted` | boolean | no | — | — | Message is encrypted. |
| `rule.exceptions.is_meeting_request` | boolean | no | — | — | Message is a meeting request. |
| `rule.exceptions.is_meeting_response` | boolean | no | — | — | Message is a meeting response. |
| `rule.exceptions.is_non_delivery_report` | boolean | no | — | — | Message is a non-delivery report. |
| `rule.exceptions.is_permission_controlled` | boolean | no | — | — | Message is rights-protected. |
| `rule.exceptions.is_read_receipt` | boolean | no | — | — | Message is a read receipt. |
| `rule.exceptions.is_signed` | boolean | no | — | — | Message is S/MIME signed. |
| `rule.exceptions.is_voicemail` | boolean | no | — | — | Message is a voicemail. |
| `rule.exceptions.not_sent_to_me` | boolean | no | — | — | You are not a recipient. |
| `rule.exceptions.sent_cc_me` | boolean | no | — | — | You are in Cc. |
| `rule.exceptions.sent_only_to_me` | boolean | no | — | — | You are the only recipient. |
| `rule.exceptions.sent_to_me` | boolean | no | — | — | You are in To. |
| `rule.exceptions.sent_to_or_cc_me` | boolean | no | — | — | You are in To or Cc. |
| `rule.is_enabled` | boolean | no | `true` | — | Whether the rule runs. |
| `rule.sequence` | integer | no | — | min 1, max 1000 | Run order (1 runs first). |
| `is_enabled` | boolean | no | — | — | set_enabled: turn the rule on (true) or off (false). |
| `position` | `top` \| `bottom` \| `up` \| `down` \| `before` \| `after` | no | — | — | reorder: new position. before/after need relative_to_rule_id. |
| `relative_to_rule_id` | string | no | — | min length 1, max length 1024 | reorder with before/after: the other rule. |
| `confirm` | boolean | no | `false` | — | Must be true to save a rule that forwards, redirects or deletes mail; set only after the user approves. |

### Output

The rule after the change.

| Field | Type |
|---|---|
| `action` | `create` \| `update` \| `set_enabled` \| `reorder` |
| `rule` | [email_rule](#type-email_rule) |
| `summary` | string |

### Microsoft Graph calls

- create: POST /me/mailFolders/inbox/messageRules
- update / set_enabled: PATCH /me/mailFolders/inbox/messageRules/{id}
- reorder: GET all rules, then PATCH sequence on each rule whose position changes

### Server-side validation

| Rule | Error returned |
|---|---|
| create needs rule.display_name, rule.conditions and rule.actions. | Invalid rule: create requires display_name, conditions and actions |
| update / set_enabled / reorder need rule_id. | Invalid rule_id: required for action 'reorder' |
| before/after need relative_to_rule_id, which must differ from rule_id. | Invalid relative_to_rule_id: required for position 'before' |
| move_to_folder / copy_to_folder must be existing mail folder IDs. | Invalid move_to_folder: folder not found |
| confirm required when the resulting rule forwards, redirects or deletes. | Invalid confirm 'False': a rule that forwards mail requires confirm=True to proceed. Expected: Explicit user confirmation |

### Example — File bills into a folder

```json
{
  "arguments": {
    "action": "create",
    "rule": {
      "display_name": "Bills",
      "conditions": {
        "from_addresses": [
          "billing@example.com"
        ]
      },
      "actions": {
        "move_to_folder": "AQMkADAwATM3ZmYAZS1kBILL",
        "stop_processing_rules": true
      }
    }
  },
  "structuredContent": {
    "action": "create",
    "rule": {
      "id": "AQAAAJ5dZqA=",
      "display_name": "Bills",
      "sequence": 3,
      "is_enabled": true,
      "conditions": {
        "from_addresses": [
          "billing@example.com"
        ]
      },
      "actions": {
        "move_to_folder": "AQMkADAwATM3ZmYAZS1kBILL",
        "stop_processing_rules": true
      },
      "exceptions": null
    },
    "summary": "Created rule 'Bills' (position 3)."
  }
}
```

<a id="calendar_forward"></a>

## `calendar_forward` — Forward Invitation

Forward a meeting invitation to people the user names, with an optional note. Requires confirm=true after the user approves.

- **Tier:** extended · **Category:** calendar · **Safety:** dangerous
- **Confirm:** always — Always required.
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `event_id` | string | yes | — | min length 1, max length 1024 | Event to forward. |
| `to` | array of string (email) | yes | — | min items 1, max items 100, unique | Recipients. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `comment` | string | no | — | max length 2000 | Note to include. |
| `confirm` | boolean | yes | `false` | — | Must be true to forward this invitation; set only after the user approves. |

### Output

Forward result.

| Field | Type |
|---|---|
| `event_id` | string |
| `status` | any |
| `recipient_count` | integer |
| `summary` | string |

### Microsoft Graph calls

- POST /me/events/{id}/forward {toRecipients, comment}

### Server-side validation

| Rule | Error returned |
|---|---|
| confirm must be true. | Invalid confirm 'False': forward invitation requires confirm=True to proceed. Expected: Explicit user confirmation |

### Example — Forward to a friend

```json
{
  "arguments": {
    "event_id": "AAMkADAwATM3INV",
    "to": [
      "sam@example.com"
    ],
    "confirm": true
  },
  "structuredContent": {
    "event_id": "AAMkADAwATM3INV",
    "status": "forwarded",
    "recipient_count": 1,
    "summary": "Forwarded 'Book club' to 1 recipient."
  }
}
```

<a id="account_list"></a>

## `account_list` — List Accounts

List the Microsoft accounts signed in to this server. Needed only when more than one account is signed in, to choose account_id.

- **Tier:** admin · **Category:** account · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

_No parameters._

### Output

Signed-in accounts.

| Field | Type |
|---|---|
| `accounts` | array of object |
| `summary` | string |

### Microsoft Graph calls

- None (local MSAL token cache).

### Example — One account

```json
{
  "arguments": {},
  "structuredContent": {
    "accounts": [
      {
        "account_id": "00000000-0000-0000-d16c-ebaa65a1624c.9188040d-6c67-4c5b-b112-36a304b66dad",
        "email": "robin@example.com",
        "display_name": "Robin"
      }
    ],
    "summary": "1 account signed in."
  }
}
```

<a id="account_auth_begin"></a>

## `account_auth_begin` — Start Sign-in

Start signing in a personal Microsoft account. Returns a web address and code for the user to enter, and an auth_session_id to pass to account_auth_complete. Prefer `uv run authenticate.py` when available.

- **Tier:** admin · **Category:** account · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=false, openWorld=true

### Input

_No parameters._

### Output

Sign-in instructions. The device code itself never leaves the server.

| Field | Type |
|---|---|
| `auth_session_id` | string |
| `verification_url` | string |
| `user_code` | string |
| `expires_in` | integer |
| `summary` | string |

### Microsoft Graph calls

- MSAL initiate_device_flow against https://login.microsoftonline.com/consumers

### Example — Begin

```json
{
  "arguments": {},
  "structuredContent": {
    "auth_session_id": "as_91b2",
    "verification_url": "https://login.microsoft.com/device",
    "user_code": "PMQDYGWPS",
    "expires_in": 900,
    "summary": "Visit https://login.microsoft.com/device and enter PMQDYGWPS."
  }
}
```

<a id="account_auth_complete"></a>

## `account_auth_complete` — Finish Sign-in

Finish a sign-in started with account_auth_begin, after the user has entered the code. Returns pending (try again shortly) or the signed-in account. Work or school accounts are rejected.

- **Tier:** admin · **Category:** account · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `auth_session_id` | string | yes | — | min length 1, max length 128 | From account_auth_begin. |

### Output

Sign-in state.

| Field | Type |
|---|---|
| `status` | `pending` \| `success` |
| `account` | object or null |
| `summary` | string |

### Microsoft Graph calls

- MSAL acquire_token_by_device_flow (single poll, exit_condition=lambda f: True)

### Server-side validation

| Rule | Error returned |
|---|---|
| Unknown or expired sessions are rejected. | Invalid auth_session_id: unknown or expired. Expected: start again with account_auth_begin |
| Work/school accounts are rejected. | Only personal Microsoft accounts are supported |

### Example — Still waiting

```json
{
  "arguments": {
    "auth_session_id": "as_91b2"
  },
  "structuredContent": {
    "status": "pending",
    "account": null,
    "summary": "Waiting for the user to enter the code."
  }
}
```

<a id="admin_cache_get"></a>

## `admin_cache_get` — Cache Status

Inspect the local encrypted cache: overall statistics (view='stats'), background tasks (view='tasks'), one task (view='task') or cache warming progress (view='warming').

- **Tier:** admin · **Category:** admin · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `view` | `stats` \| `tasks` \| `task` \| `warming` | yes | — | — | What to show. |
| `task_id` | string | no | — | min length 1, max length 128 | view='task': task ID. |
| `status` | `queued` \| `running` \| `completed` \| `failed` | no | — | — | view='tasks': filter by status. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `limit` | integer | no | `50` | min 1, max 200 | view='tasks': most tasks to return. |

### Output

Cache information; fields for other views are null. view='task' returns a one-element tasks list.

| Field | Type |
|---|---|
| `view` | `stats` \| `tasks` \| `task` \| `warming` |
| `stats` | object or null |
| `tasks` | array of object or null |
| `warming` | object or null |
| `summary` | string |

### Microsoft Graph calls

- None (local cache database).

### Server-side validation

| Rule | Error returned |
|---|---|
| view='task' requires task_id. | Invalid task_id: required for view='task' |

### Example — Warming status

```json
{
  "arguments": {
    "view": "warming"
  },
  "structuredContent": {
    "view": "warming",
    "stats": null,
    "tasks": null,
    "warming": {
      "status": "disabled",
      "is_warming": false,
      "operations_total": 0,
      "operations_completed": 0,
      "operations_skipped": 0,
      "operations_failed": 0,
      "progress_percent": 0.0
    },
    "summary": "Cache warming is disabled."
  }
}
```

<a id="admin_cache_invalidate"></a>

## `admin_cache_invalidate` — Clear Cache

Remove cached results so the next call fetches fresh data: for one resource type or all, for one account or all.

- **Tier:** admin · **Category:** admin · **Safety:** moderate
- **Confirm:** never
- **Annotations:** readOnly=false, destructive=false, idempotent=true, openWorld=true

### Input

| Parameter | Type | Required | Default | Constraints | Description |
|---|---|---|---|---|---|
| `scope` | `email` \| `email_folder` \| `email_rule` \| `event` \| `calendar` \| `contact` \| `contact_folder` \| `drive_item` \| `all` | yes | — | — | Resource type to clear, or all. |
| `account_id` | string | no | — | min length 1, max length 320 | Account ID or email address. Omit when only one account is signed in. |
| `reason` | string | no | — | max length 500 | Why (recorded in the audit log). |

### Output

Invalidation result.

| Field | Type |
|---|---|
| `scope` | string |
| `entries_removed` | integer |
| `summary` | string |

### Microsoft Graph calls

- None (local cache database).

### Example — Clear email cache

```json
{
  "arguments": {
    "scope": "email",
    "reason": "stale inbox"
  },
  "structuredContent": {
    "scope": "email",
    "entries_removed": 12,
    "summary": "Removed 12 cached email entries."
  }
}
```

<a id="admin_server_info"></a>

## `admin_server_info` — Server Info

Report the server version, supported MCP protocol versions and enabled toolsets.

- **Tier:** admin · **Category:** admin · **Safety:** safe
- **Confirm:** never
- **Annotations:** readOnly=true, destructive=false, idempotent=true, openWorld=true

### Input

_No parameters._

### Output

Server information.

| Field | Type |
|---|---|
| `version` | string |
| `protocol_versions` | array of string |
| `toolsets_enabled` | array of `core` \| `extended` \| `admin` |
| `tool_count` | integer |
| `cache_enabled` | boolean |
| `summary` | string |

### Microsoft Graph calls

- None.

### Example — Info

```json
{
  "arguments": {},
  "structuredContent": {
    "version": "1.0.0",
    "protocol_versions": [
      "2025-06-18"
    ],
    "toolsets_enabled": [
      "core",
      "extended",
      "admin"
    ],
    "tool_count": 29,
    "cache_enabled": true,
    "summary": "m365-mcp 1.0.0, 29 tools."
  }
}
```

## Result types

Shared output projections (`$defs`).

<a id="type-attachment_meta"></a>

### `attachment_meta`

Email attachment metadata (content via m365_get_content).

| Field | Type |
|---|---|
| `id` | string |
| `name` | string |
| `size` | integer |
| `content_type` | string or null |
| `is_inline` | boolean |

<a id="type-attendee"></a>

### `attendee`

Event attendee and their response.

| Field | Type |
|---|---|
| `name` | string or null |
| `address` | string |
| `type` | `required` \| `optional` \| `resource` |
| `response` | `none` \| `organizer` \| `tentativelyAccepted` \| `accepted` \| `declined` \| `notResponded` |

<a id="type-calendar"></a>

### `calendar`

Calendar.

| Field | Type |
|---|---|
| `id` | string |
| `name` | string |
| `is_default` | boolean |
| `can_edit` | boolean |
| `color` | string or null |

<a id="type-contact"></a>

### `contact`

Contact.

| Field | Type |
|---|---|
| `id` | string |
| `display_name` | string or null |
| `given_name` | string or null |
| `surname` | string or null |
| `emails` | array of string |
| `mobile_phone` | string or null |
| `business_phones` | array of string |
| `home_phones` | array of string |
| `company_name` | string or null |
| `job_title` | string or null |
| `department` | string or null |
| `folder_id` | string or null |

<a id="type-contact_folder"></a>

### `contact_folder`

Contact folder.

| Field | Type |
|---|---|
| `id` | string |
| `display_name` | string |
| `parent_id` | string or null |

<a id="type-drive_item"></a>

### `drive_item`

OneDrive file or folder. children is populated only in tree mode.

| Field | Type |
|---|---|
| `id` | string |
| `name` | string |
| `item_type` | `file` \| `folder` |
| `path` | string or null |
| `parent_id` | string or null |
| `size` | integer |
| `modified_at` | string (date-time) or null |
| `created_at` | string (date-time) or null |
| `mime_type` | string or null |
| `child_count` | integer or null |
| `web_url` | string or null |
| `shared` | boolean |
| `children` | array of [drive_item](#type-drive_item) or null |

<a id="type-email_detail"></a>

### `email_detail`

Email from m365_get. body is plain text and untrusted.

| Field | Type |
|---|---|
| `id` | string |
| `conversation_id` | string or null |
| `subject` | string or null |
| `from` | [recipient](#type-recipient) or null |
| `to` | array of [recipient](#type-recipient) |
| `cc` | array of [recipient](#type-recipient) |
| `received_at` | string (date-time) or null |
| `is_read` | boolean |
| `has_attachments` | boolean |
| `importance` | `low` \| `normal` \| `high` |
| `flag_status` | `not_flagged` \| `flagged` \| `complete` |
| `categories` | array of string |
| `preview` | string or null |
| `folder_id` | string or null |
| `bcc` | array of [recipient](#type-recipient) |
| `body` | string or null |
| `body_truncated` | boolean |
| `attachments` | array of [attachment_meta](#type-attachment_meta) |
| `web_link` | string or null |

<a id="type-email_folder"></a>

### `email_folder`

Mail folder. children is populated only in tree mode (recursive=true).

| Field | Type |
|---|---|
| `id` | string |
| `display_name` | string |
| `parent_id` | string or null |
| `unread_count` | integer |
| `total_count` | integer |
| `child_count` | integer |
| `children` | array of [email_folder](#type-email_folder) or null |

<a id="type-email_rule"></a>

### `email_rule`

Inbox rule.

| Field | Type |
|---|---|
| `id` | string |
| `display_name` | string |
| `sequence` | integer |
| `is_enabled` | boolean |
| `conditions` | [rule_predicates](#type-rule_predicates) |
| `actions` | [rule_actions](#type-rule_actions) |
| `exceptions` | [rule_predicates](#type-rule_predicates) or null |

<a id="type-email_summary"></a>

### `email_summary`

Email in list/search results. subject and preview are third-party text: untrusted.

| Field | Type |
|---|---|
| `id` | string |
| `conversation_id` | string or null |
| `subject` | string or null |
| `from` | [recipient](#type-recipient) or null |
| `to` | array of [recipient](#type-recipient) |
| `cc` | array of [recipient](#type-recipient) |
| `received_at` | string (date-time) or null |
| `is_read` | boolean |
| `has_attachments` | boolean |
| `importance` | `low` \| `normal` \| `high` |
| `flag_status` | `not_flagged` \| `flagged` \| `complete` |
| `categories` | array of string |
| `preview` | string or null |
| `folder_id` | string or null |

<a id="type-event_detail"></a>

### `event_detail`

Event from m365_get. recurrence is a read-only human summary.

| Field | Type |
|---|---|
| `id` | string |
| `subject` | string or null |
| `start` | string (date-time) |
| `end` | string (date-time) |
| `time_zone` | string |
| `is_all_day` | boolean |
| `location` | string or null |
| `organizer` | [recipient](#type-recipient) or null |
| `is_organizer` | boolean |
| `my_response` | `none` \| `organizer` \| `tentativelyAccepted` \| `accepted` \| `declined` \| `notResponded` |
| `show_as` | `free` \| `tentative` \| `busy` \| `oof` \| `workingElsewhere` \| `unknown` |
| `attendee_count` | integer |
| `calendar_id` | string or null |
| `preview` | string or null |
| `body` | string or null |
| `body_truncated` | boolean |
| `attendees` | array of [attendee](#type-attendee) |
| `web_link` | string or null |
| `recurrence` | string or null |

<a id="type-event_summary"></a>

### `event_summary`

Event in list/search results. subject, location and preview are untrusted.

| Field | Type |
|---|---|
| `id` | string |
| `subject` | string or null |
| `start` | string (date-time) |
| `end` | string (date-time) |
| `time_zone` | string |
| `is_all_day` | boolean |
| `location` | string or null |
| `organizer` | [recipient](#type-recipient) or null |
| `is_organizer` | boolean |
| `my_response` | `none` \| `organizer` \| `tentativelyAccepted` \| `accepted` \| `declined` \| `notResponded` |
| `show_as` | `free` \| `tentative` \| `busy` \| `oof` \| `workingElsewhere` \| `unknown` |
| `attendee_count` | integer |
| `calendar_id` | string or null |
| `preview` | string or null |

<a id="type-operation"></a>

### `operation`

Status of a background OneDrive copy.

| Field | Type |
|---|---|
| `operation_id` | string |
| `status` | `in_progress` \| `completed` \| `failed` |
| `percent_complete` | number or null |
| `resource_id` | string or null |
| `error` | string or null |

<a id="type-recipient"></a>

### `recipient`

A person's name and address.

| Field | Type |
|---|---|
| `name` | string or null |
| `address` | string |

<a id="type-rule_actions"></a>

### `rule_actions`

Rule actions, same keys as the input schema.

Keys: `move_to_folder`, `copy_to_folder`, `assign_categories`, `mark_as_read`, `mark_importance`, `stop_processing_rules`, `forward_to`, `forward_as_attachment_to`, `redirect_to`, `delete`, `permanent_delete`

<a id="type-rule_predicates"></a>

### `rule_predicates`

Rule conditions, same keys as the input schema.

Keys: `subject_contains`, `body_contains`, `body_or_subject_contains`, `sender_contains`, `recipient_contains`, `header_contains`, `categories`, `from_addresses`, `sent_to_addresses`, `has_attachments`, `importance`, `sensitivity`, `message_action_flag`, `within_size_range`, `is_approval_request`, `is_automatic_forward`, `is_automatic_reply`, `is_encrypted`, `is_meeting_request`, `is_meeting_response`, `is_non_delivery_report`, `is_permission_controlled`, `is_read_receipt`, `is_signed`, `is_voicemail`, `not_sent_to_me`, `sent_cc_me`, `sent_only_to_me`, `sent_to_me`, `sent_to_or_cc_me`

