{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "contact_delete",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "contact_id": {
      "type": "string",
      "description": "Identifier of the contact to delete permanently."
    },
    "account_id": {
      "type": "string",
      "description": "Microsoft account ID used to perform the deletion."
    },
    "confirm": {
      "type": "boolean",
      "default": false,
      "const": true,
      "description": "Must be true to confirm irreversible deletion."
    }
  },
  "required": ["contact_id", "account_id", "confirm"]
}
